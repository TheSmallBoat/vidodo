# 工作任务卡、里程碑与路线图

日期：2026-04-01
状态：Draft
前置阅读：14-MVP技术架构图与Phase0实施清单.md，15-功能模块设计方案总览.md，22-编码前置完备性审计报告.md

## 1. 文档目的

本文将 14 号文档中的 Workstream A-F 拆解为可分配、可追踪的工作任务卡，并定义里程碑节点与依赖关系。

## 2. 任务卡规范

### 2.1 任务卡字段

| 字段 | 说明 |
| --- | --- |
| 卡号 | WS{工作流字母}-{序号}，例如 WSA-01 |
| 标题 | 动词开头的简短描述 |
| 工作流 | 所属 Workstream |
| 依赖 | 前置任务卡号 |
| 交付物 | 必须产出的工件 |
| 验收标准 | 可客观判断的完成条件 |
| 角色 | 推荐执行角色 |
| 规模 | S / M / L（1-2 / 3-5 / 5+ 工作日） |

### 2.2 任务状态

- `todo` → `in_progress` → `review` → `done`
- 允许 `blocked`（需记录阻塞原因与依赖卡号）

---

## 3. Workstream A：Schema 与工件基础

### WSA-01 定义通用对象头 Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | 无 |
| 交付物 | `schemas/common/object-header.v0.json` |
| 验收标准 | 所有一等对象的 id、type、version、revision、schema、source、provenance 字段有正式 JSON Schema；05 号文档示例通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-02 定义 SetPlan Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/planning/set-plan.v0.json` |
| 验收标准 | 11 号文档示例 SetPlan 通过校验；缺少 show_id 时校验失败 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-03 定义 AudioDsl Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/planning/audio-dsl.v0.json` |
| 验收标准 | 05 号文档示例 AudioDsl 通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-04 定义 VisualDsl Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/planning/visual-dsl.v0.json` |
| 验收标准 | 05 号文档示例 VisualDsl 通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-05 定义 ConstraintSet Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/planning/constraint-set.v0.json` |
| 验收标准 | 05 号文档 ConstraintSet 字段完整覆盖 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-06 定义 AssetRecord / Asset IR Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/asset/asset-record.v0.json`、`schemas/ir/asset-ir.v0.json` |
| 验收标准 | 06 号文档示例 AssetRecord 和 05 号文档 Asset IR 示例通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-07 定义 Structure IR / Performance IR / Visual IR Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/ir/structure-ir.v0.json`、`schemas/ir/performance-ir.v0.json`、`schemas/ir/visual-ir.v0.json` |
| 验收标准 | 05 号文档各层 IR 示例通过校验 |
| 角色 | Core Engineer |
| 规模 | M |

### WSA-08 定义 TimelineEntry Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-07 |
| 交付物 | `schemas/ir/timeline-entry.v0.json` |
| 验收标准 | 05 号文档 Unified Timeline Entry 示例通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-09 定义 LivePatchProposal Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/patch/live-patch-proposal.v0.json` |
| 验收标准 | 08 号文档与 11 号文档 patch 示例通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-10 定义 Trace Bundle Manifest Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/trace/trace-manifest.v0.json` |
| 验收标准 | 07 号文档 Manifest 示例通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-11 定义 Runtime Event 通用头与六类 Event Payload Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/runtime/event-header.v0.json`、`schemas/runtime/timing-event.v0.json`、`schemas/runtime/audio-event.v0.json`、`schemas/runtime/visual-event.v0.json`、`schemas/runtime/semantic-event.v0.json`、`schemas/runtime/patch-event.v0.json`、`schemas/runtime/transport-event.v0.json` |
| 验收标准 | 04 号文档所有事件示例通过校验 |
| 角色 | Core Engineer |
| 规模 | M |

### WSA-12 定义 CapabilityResponse Envelope Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-01 |
| 交付物 | `schemas/common/response-envelope.v0.json`、`schemas/common/diagnostic.v0.json` |
| 验收标准 | 10 号文档 envelope 示例与 23 号文档 diagnostic 结构均通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSA-13 建立 Artifact Store 目录结构与 SQLite 索引表初始化

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-06 |
| 交付物 | artifact 目录结构创建脚本、SQLite schema DDL 文件 |
| 验收标准 | `artifacts/` 目录按 14 号文档 §5 布局生成；`registry.db` 初始化成功并包含 assets、ingestion_runs、analysis_jobs 表 |
| 角色 | Tools Engineer |
| 规模 | S |

### WSA-14 建立 Schema 校验集成测试

| 字段 | 值 |
| --- | --- |
| 工作流 | A |
| 依赖 | WSA-02 ~ WSA-12 |
| 交付物 | `tests/schema/` 目录下正例/反例 fixture 与自动化校验脚本 |
| 验收标准 | CI 可自动运行全部 schema 正例通过、反例拒绝 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 4. Workstream B：资产接入与分析

### WSB-01 实现 Candidate Scanner

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSA-06, WSA-13 |
| 交付物 | `python/ingestion/scanner.py` |
| 验收标准 | 扫描指定目录输出 IngestionCandidate 列表 JSON |
| 角色 | Analysis Engineer |
| 规模 | S |

### WSB-02 实现 Fingerprint Builder

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-01 |
| 交付物 | `python/ingestion/fingerprint.py` |
| 验收标准 | 相同文件重复生成相同 content_hash；不同文件 hash 不同 |
| 角色 | Analysis Engineer |
| 规模 | S |

### WSB-03 实现音频素材 Normalize

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-02 |
| 交付物 | `python/ingestion/normalize_audio.py` |
| 验收标准 | 输出统一 sample rate、统一声道布局的 normalized 文件 |
| 角色 | Analysis Engineer |
| 规模 | S |

### WSB-04 实现视觉素材最小注册

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-02 |
| 交付物 | `python/ingestion/register_visual.py` |
| 验收标准 | shader / texture 文件可登记到 registry 并输出 AssetRecord |
| 角色 | Analysis Engineer |
| 规模 | S |

### WSB-05 实现 SQLite 资产索引写入与查询

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSA-13, WSB-03 |
| 交付物 | `python/ingestion/registry.py` 或 Rust `crates/storage/` |
| 验收标准 | avctl asset list 可输出已发布资产；按 tag/kind 过滤正确 |
| 角色 | Tools Engineer |
| 规模 | M |

### WSB-06 实现 beat_track 分析任务

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-03 |
| 交付物 | `python/analyzers/beat_track.py`、分析结果 JSON |
| 验收标准 | 输出 tempo、downbeat 信息并写入 analysis cache |
| 角色 | Analysis Engineer |
| 规模 | M |

### WSB-07 实现 section_segmentation 分析任务

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-03 |
| 交付物 | `python/analyzers/section_seg.py`、分析结果 JSON |
| 验收标准 | 输出 section boundary 信息并写入 analysis cache |
| 角色 | Analysis Engineer |
| 规模 | M |

### WSB-08 实现 avctl asset ingest 命令

| 字段 | 值 |
| --- | --- |
| 工作流 | B |
| 依赖 | WSB-05, WSB-06 |
| 交付物 | `apps/avctl/` asset ingest 子命令 |
| 验收标准 | 一条命令完成 discover → fingerprint → normalize → analyze → publish 全链路 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 5. Workstream C：Compiler 与 Timeline

### WSC-01 实现 Rust IR 类型定义

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSA-07, WSA-08 |
| 交付物 | `crates/ir/src/` 下 SetPlan、AudioDsl、VisualDsl、StructureIR、PerformanceIR、VisualIR、TimelineEntry 类型 |
| 验收标准 | 类型可序列化/反序列化为对应 JSON；与 schema 定义一致 |
| 角色 | Core Engineer |
| 规模 | M |

### WSC-02 实现 Planning DSL 读取与解析

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSC-01 |
| 交付物 | `crates/compiler/src/parser.rs` |
| 验收标准 | 读取 SetPlan + AudioDsl + VisualDsl JSON 并反序列化为内部类型 |
| 角色 | Core Engineer |
| 规模 | S |

### WSC-03 实现基础 Validator

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSC-01, WSA-12 |
| 交付物 | `crates/validator/src/` |
| 验收标准 | schema 校验、必填字段、asset 引用可解析、资源预算初步检查均可输出结构化 diagnostic |
| 角色 | Core Engineer |
| 规模 | M |

### WSC-04 实现 Compiler 主链路

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSC-02, WSC-03, WSB-05 |
| 交付物 | `crates/compiler/src/compile.rs` |
| 验收标准 | 输入 plan bundle → 输出 Structure IR + Performance IR + Visual IR + Unified Timeline；同一输入重复编译结果 bit-for-bit 稳定 |
| 角色 | Core Engineer |
| 规模 | L |

### WSC-05 实现 Revision Manager

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSC-04, WSA-13 |
| 交付物 | `crates/compiler/src/revision.rs` |
| 验收标准 | 可分配 candidate revision、publish、归档；RevisionRecord 写入 SQLite 并可查询 |
| 角色 | Core Engineer |
| 规模 | M |

### WSC-06 实现 avctl plan validate / plan submit / compile run 命令

| 字段 | 值 |
| --- | --- |
| 工作流 | C |
| 依赖 | WSC-04, WSC-05 |
| 交付物 | `apps/avctl/` plan 与 compile 子命令 |
| 验收标准 | CLI 输出标准 envelope JSON，validation 失败时返回结构化 diagnostics |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 6. Workstream D：Scheduler 与双 Runtime 骨架

### WSD-01 实现 Musical Clock

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSC-04 |
| 交付物 | `crates/scheduler/src/clock.rs` |
| 验收标准 | 给定 tempo + time_signature 可正确推进 beat → bar → phrase → section；离线模式时钟可虚拟化 |
| 角色 | Core Engineer |
| 规模 | M |

### WSD-02 实现 Lookahead Queue 与事件发布

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSD-01, WSA-11 |
| 交付物 | `crates/scheduler/src/lookahead.rs` |
| 验收标准 | timeline entries 按 musical_time 排入队列并在到期时发布为 runtime event |
| 角色 | Core Engineer |
| 规模 | M |

### WSD-03 实现最小 Show State

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSD-01 |
| 交付物 | `crates/scheduler/src/show_state.rs` |
| 验收标准 | 包含 04 号文档定义的最小字段集；可序列化为 JSON snapshot |
| 角色 | Core Engineer |
| 规模 | S |

### WSD-04 实现 Audio Runtime Client 骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSD-02, WSA-11 |
| 交付物 | `crates/scheduler/src/audio_client.rs` 或 `apps/core-service/` 内桥接模块 |
| 验收标准 | 消费 audio event 并翻译为 mock backend 命令；收集 ack 并上报 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSD-05 实现 Visual Runtime 骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSD-02, WSA-11 |
| 交付物 | `apps/visual-runtime/` |
| 验收标准 | 消费 visual event + timing event；最小 scene 可渲染纯色 + 单 uniform；输出 scene ack |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSD-06 实现 avctl run start / run status 命令

| 字段 | 值 |
| --- | --- |
| 工作流 | D |
| 依赖 | WSD-03, WSD-04, WSD-05 |
| 交付物 | `apps/avctl/` run 子命令 |
| 验收标准 | 启动 run 后可查询 show state；section/phrase 进度正确推进 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 7. Workstream E：Trace 与导出

### WSE-01 实现 Trace Writer

| 字段 | 值 |
| --- | --- |
| 工作流 | E |
| 依赖 | WSD-02 |
| 交付物 | `crates/trace/src/writer.rs` |
| 验收标准 | event_record 以 JSONL 写入；run manifest 与 trace bundle 目录结构正确生成 |
| 角色 | Tools Engineer |
| 规模 | M |

### WSE-02 实现 Patch Decision 与 Resource Sample 记录

| 字段 | 值 |
| --- | --- |
| 工作流 | E |
| 依赖 | WSE-01 |
| 交付物 | patch_decision.jsonl 与 resource_sample.jsonl 写入能力 |
| 验收标准 | patch 与 resource 数据随 run 同步记录 |
| 角色 | Tools Engineer |
| 规模 | S |

### WSE-03 实现最小离线导出

| 字段 | 值 |
| --- | --- |
| 工作流 | E |
| 依赖 | WSD-04 |
| 交付物 | offline export 流程脚本或模块 |
| 验收标准 | offline run 完成后可输出至少一个音频导出文件，并关联到 trace bundle |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSE-04 实现 avctl trace show / trace events 命令

| 字段 | 值 |
| --- | --- |
| 工作流 | E |
| 依赖 | WSE-01 |
| 交付物 | `apps/avctl/` trace 子命令 |
| 验收标准 | 按 run_id 查询 manifest；按 bar 区间查询事件记录 |
| 角色 | Tools Engineer |
| 规模 | S |

---

## 8. Workstream F：Patch 与回退最小闭环

### WSF-01 实现 Patch Manager 核心状态机

| 字段 | 值 |
| --- | --- |
| 工作流 | F |
| 依赖 | WSC-05, WSD-02 |
| 交付物 | `crates/patch-manager/src/` |
| 验收标准 | Proposed → Authorized → Validated → Staged → Activated → Committed 状态流转正确；Rejected / RolledBack 分支可达 |
| 角色 | Core Engineer |
| 规模 | L |

### WSF-02 实现 patch.check 预检

| 字段 | 值 |
| --- | --- |
| 工作流 | F |
| 依赖 | WSF-01, WSC-03 |
| 交付物 | patch check 能力 |
| 验收标准 | 窗口检查、锁定区间检查、资产预热检查、资源预算检查均输出结构化 diagnostic |
| 角色 | Core Engineer |
| 规模 | M |

### WSF-03 实现 patch.submit 与生效流程

| 字段 | 值 |
| --- | --- |
| 工作流 | F |
| 依赖 | WSF-02, WSD-02 |
| 交付物 | patch submit、activate 能力 |
| 验收标准 | local_content patch 在 next_phrase_boundary 生效；新 effective_revision 产生 |
| 角色 | Core Engineer |
| 规模 | M |

### WSF-04 实现 Deferred Rollback

| 字段 | 值 |
| --- | --- |
| 工作流 | F |
| 依赖 | WSF-03, WSE-02 |
| 交付物 | rollback 能力 |
| 验收标准 | 模拟异常后触发 deferred rollback；effective_revision 恢复到 fallback_revision；rollback 决策写入 trace |
| 角色 | Core Engineer |
| 规模 | M |

### WSF-05 实现 avctl patch check / submit / rollback 命令

| 字段 | 值 |
| --- | --- |
| 工作流 | F |
| 依赖 | WSF-03, WSF-04 |
| 交付物 | `apps/avctl/` patch 子命令 |
| 验收标准 | CLI 可完成 check → submit → rollback 演示流程 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 9. Workstream G：端到端集成与验收

### WSG-01 准备最小测试素材包

| 字段 | 值 |
| --- | --- |
| 工作流 | G |
| 依赖 | WSA-14 |
| 交付物 | `tests/fixtures/assets/` 下 4 个音频 + 2 个视觉素材 |
| 验收标准 | 素材可被 ingest 并通过分析 |
| 角色 | Analysis Engineer |
| 规模 | S |

### WSG-02 准备 Plan Bundle Fixture

| 字段 | 值 |
| --- | --- |
| 工作流 | G |
| 依赖 | WSA-02 ~ WSA-05, WSG-01 |
| 交付物 | `tests/fixtures/plans/` 下 SetPlan + AudioDsl + VisualDsl + ConstraintSet |
| 验收标准 | 全部通过 schema 校验；引用的 asset 均存在于测试素材包中 |
| 角色 | Core Engineer |
| 规模 | S |

### WSG-03 端到端集成测试

| 字段 | 值 |
| --- | --- |
| 工作流 | G |
| 依赖 | WSB-08, WSC-06, WSD-06, WSE-04, WSF-05 |
| 交付物 | `tests/e2e/` 集成测试脚本 |
| 验收标准 | 覆盖 14 号文档 §10 演示脚本全部 10 步；每步有可验证的 artifact 输出 |
| 角色 | Tools Engineer |
| 规模 | L |

### WSG-04 Evaluation 占位输出

| 字段 | 值 |
| --- | --- |
| 工作流 | G |
| 依赖 | WSE-01 |
| 交付物 | 最小 evaluation report 生成能力 |
| 验收标准 | 从 trace bundle 生成包含 sync_score、transition_score 的 JSON 报告 |
| 角色 | Core Engineer |
| 规模 | S |

---

## 10. 任务依赖图

```text
WSA-01 ─┬─ WSA-02 ─────────────────────────────────┐
        ├─ WSA-03 ─────────────────────────────────┤
        ├─ WSA-04 ─────────────────────────────────┤
        ├─ WSA-05 ─────────────────────────────────┤
        ├─ WSA-06 ──┬── WSA-13 ── WSB-05           │
        │           │                              │
        ├─ WSA-07 ──┼── WSA-08                     │
        │           │                              │
        ├─ WSA-09 ──┤                              │
        ├─ WSA-10 ──┤                              │
        ├─ WSA-11 ──┤                              │
        └─ WSA-12 ──┴─────────────── WSA-14 ───── WSG-01 ── WSG-02
                                                              │
WSB-01 ── WSB-02 ─┬─ WSB-03 ── WSB-06 ──┐                     │
                  └─ WSB-04             ├── WSB-08            │
                  WSB-05 ───────────────┘     │               │
                                              │               │
WSC-01 ── WSC-02 ── WSC-04 ── WSC-05 ── WSC-06                │
           WSC-03 ──┘                         │               │
                                              │               │
WSD-01 ─┬─ WSD-02 ──┬─ WSD-04                 │               │
        │           ├─ WSD-05                 │               │
        └─ WSD-03   │                         │               │
                    └─ WSD-06 ────────────────┤               │
                                              │               │
WSE-01 ─┬─ WSE-02                             │               │
        ├─ WSE-04                             │               │
        └─ WSG-04                             │               │
WSE-03                                        │               │
                                              │               │
WSF-01 ── WSF-02 ── WSF-03 ── WSF-04 ── WSF-05                │
                                              │               │
                                              └───── WSG-03 ◄─┘
```

---

## 11. 里程碑定义

### M0：Schema 与工件面固定

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSA-01 ~ WSA-14 全部 done |
| 验收标准 | 所有 JSON Schema 文件存在且正例/反例测试通过；artifact store 初始化脚本可运行；CI schema 测试绿灯 |
| 交付物 | `schemas/` 完整目录、`tests/schema/` fixture 集、SQLite DDL |
| 关键判断 | 此里程碑完成前不应进入 Workstream C/D 编码 |

### M1：资产可接入

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSB-01 ~ WSB-08 全部 done |
| 验收标准 | avctl asset ingest 从固定目录完成导入；avctl asset list 可输出资产；编译器可解析 asset 引用 |
| 交付物 | ingestion pipeline、registry、avctl asset 命令 |

### M2：编译可运行

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSC-01 ~ WSC-06 全部 done |
| 验收标准 | avctl plan submit + compile run 输出 candidate revision 与 timeline 工件；重复编译结果稳定 |
| 交付物 | IR 类型、validator、compiler、revision manager、avctl plan/compile 命令 |

### M3：双 Runtime 可执行

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSD-01 ~ WSD-06 全部 done |
| 验收标准 | avctl run start 启动后 audio + visual runtime 均收到 timing/section 事件；show state 可查询 |
| 交付物 | scheduler、musical clock、audio client、visual runtime 骨架 |

### M4：Trace 可追踪

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSE-01 ~ WSE-04 全部 done |
| 验收标准 | run 结束后 trace bundle 目录完整；avctl trace events 可按 bar 范围查询 |
| 交付物 | trace writer、trace 命令、最小离线导出 |

### M5：Patch 闭环

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSF-01 ~ WSF-05 全部 done |
| 验收标准 | 一次 local_content patch 生效；一次 deferred rollback 成功；trace 中可查到 patch 与 rollback 记录 |
| 交付物 | patch manager、patch 命令 |

### M6：Phase 0 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSG-01 ~ WSG-04 全部 done |
| 验收标准 | 14 号文档 §10 演示脚本 10 步全通过；12 号文档 §12 验收标准 5 条全满足 |
| 交付物 | 测试素材包、plan fixture、e2e 测试脚本、evaluation 占位报告 |

---

## 12. 路线图

```text
Phase 0 推荐时间顺序（不标绝对日期，以里程碑为锚点）：

Round 1 ─── M0: Schema 与工件面固定
  │         WSA-01 ~ WSA-14
  │
Round 2 ─── M1: 资产可接入
  │         WSB-01 ~ WSB-08（与 WSC-01 可并行启动）
  │
Round 3 ─── M2: 编译可运行
  │         WSC-01 ~ WSC-06
  │
Round 4 ─── M3: 双 Runtime 可执行
  │         WSD-01 ~ WSD-06
  │
Round 5 ─── M4: Trace 可追踪
  │         WSE-01 ~ WSE-04（可与 Round 4 部分并行）
  │
Round 6 ─── M5: Patch 闭环
  │         WSF-01 ~ WSF-05
  │
Round 7 ─── M6: Phase 0 验收
              WSG-01 ~ WSG-04

并行机会：
  - WSB 与 WSA 后期可交叉推进
  - WSE 与 WSD 可部分并行
  - WSG-01/02 可在 M0 后立即准备
```

---

## 13. 任务卡总览

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 |
| --- | --- | --- | --- | --- |
| WSA-01 | 定义通用对象头 Schema | A | S | M0 |
| WSA-02 | 定义 SetPlan Schema | A | S | M0 |
| WSA-03 | 定义 AudioDsl Schema | A | S | M0 |
| WSA-04 | 定义 VisualDsl Schema | A | S | M0 |
| WSA-05 | 定义 ConstraintSet Schema | A | S | M0 |
| WSA-06 | 定义 AssetRecord / Asset IR Schema | A | S | M0 |
| WSA-07 | 定义 Structure/Performance/Visual IR Schema | A | M | M0 |
| WSA-08 | 定义 TimelineEntry Schema | A | S | M0 |
| WSA-09 | 定义 LivePatchProposal Schema | A | S | M0 |
| WSA-10 | 定义 Trace Bundle Manifest Schema | A | S | M0 |
| WSA-11 | 定义 Runtime Event Schema 全套 | A | M | M0 |
| WSA-12 | 定义 Envelope 与 Diagnostic Schema | A | S | M0 |
| WSA-13 | 建立 Artifact Store 与 SQLite 初始化 | A | S | M0 |
| WSA-14 | 建立 Schema 校验集成测试 | A | M | M0 |
| WSB-01 | 实现 Candidate Scanner | B | S | M1 |
| WSB-02 | 实现 Fingerprint Builder | B | S | M1 |
| WSB-03 | 实现音频素材 Normalize | B | S | M1 |
| WSB-04 | 实现视觉素材最小注册 | B | S | M1 |
| WSB-05 | 实现 SQLite 资产索引写入与查询 | B | M | M1 |
| WSB-06 | 实现 beat_track 分析任务 | B | M | M1 |
| WSB-07 | 实现 section_segmentation 分析任务 | B | M | M1 |
| WSB-08 | 实现 avctl asset ingest 命令 | B | M | M1 |
| WSC-01 | 实现 Rust IR 类型定义 | C | M | M2 |
| WSC-02 | 实现 Planning DSL 读取与解析 | C | S | M2 |
| WSC-03 | 实现基础 Validator | C | M | M2 |
| WSC-04 | 实现 Compiler 主链路 | C | L | M2 |
| WSC-05 | 实现 Revision Manager | C | M | M2 |
| WSC-06 | 实现 avctl plan/compile 命令 | C | M | M2 |
| WSD-01 | 实现 Musical Clock | D | M | M3 |
| WSD-02 | 实现 Lookahead Queue 与事件发布 | D | M | M3 |
| WSD-03 | 实现最小 Show State | D | S | M3 |
| WSD-04 | 实现 Audio Runtime Client 骨架 | D | M | M3 |
| WSD-05 | 实现 Visual Runtime 骨架 | D | L | M3 |
| WSD-06 | 实现 avctl run start/status 命令 | D | M | M3 |
| WSE-01 | 实现 Trace Writer | E | M | M4 |
| WSE-02 | 实现 Patch Decision 与 Resource Sample 记录 | E | S | M4 |
| WSE-03 | 实现最小离线导出 | E | M | M4 |
| WSE-04 | 实现 avctl trace show/events 命令 | E | S | M4 |
| WSF-01 | 实现 Patch Manager 核心状态机 | F | L | M5 |
| WSF-02 | 实现 patch.check 预检 | F | M | M5 |
| WSF-03 | 实现 patch.submit 与生效流程 | F | M | M5 |
| WSF-04 | 实现 Deferred Rollback | F | M | M5 |
| WSF-05 | 实现 avctl patch 命令 | F | M | M5 |
| WSG-01 | 准备最小测试素材包 | G | S | M6 |
| WSG-02 | 准备 Plan Bundle Fixture | G | S | M6 |
| WSG-03 | 端到端集成测试 | G | L | M6 |
| WSG-04 | Evaluation 占位输出 | G | S | M6 |

Phase 0 共计 44 张任务卡，覆盖 7 个 Workstream 和 7 个里程碑。全部 done（2026-04-02）。

---

# Phase 1：能力层服务化与适配扩展

## 14. Phase 1 目标

Phase 0 证明了最小闭环（外部规划 → 编译 → 执行 → trace → patch → 评估）可工作。

Phase 1 的目标是：

1. 把 avctl 的业务语义提升为统一能力层（Capability Layer），使 CLI 和 MCP 共享同一能力面。
2. 用 HTTP 服务化包装能力层，使外部 Agent 可远程调用。
3. 实现 MCP 工具面，使 Agent 可发现、校验和提交结构化请求。
4. 扩展灯光运行时骨架，证明三通道（音频 + 视觉 + 灯光）统一调度可行。

Phase 1 明确不做的事：

- 不做真实硬件后端集成（Phase 2）
- 不做分布式多节点运行时
- 不做 GUI 产品
- 不做完整权限体系

## 15. Workstream H：Capability Layer

### WSH-01 定义 Capability 类型与 Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | M6 (Phase 0 complete) |
| 交付物 | `crates/ir/` 中 CapabilityDescriptor、CapabilityRequest、CapabilityResponse、OperationTicket 类型；`schemas/capability/capability-descriptor.v0.json` |
| 验收标准 | 类型可 serde 往返；schema fixture 通过校验；CapabilityDescriptor 描述 12+ 能力命名空间 |
| 角色 | Core Engineer |
| 规模 | M |

### WSH-02 实现 capability crate 与能力注册表

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-01 |
| 交付物 | `crates/capability/` — 注册表、路由器、请求标准化 |
| 验收标准 | 注册 12+ 能力描述符；按 capability id 路由返回对应描述符；未知 capability 返回结构化错误；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSH-03 迁移 avctl 到 capability 路由

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-02 |
| 交付物 | `apps/avctl/` 命令分发通过 capability router；新增 `avctl system capabilities` 命令 |
| 验收标准 | 所有现有 E2E 测试不变绿；`avctl system capabilities` 输出 12+ 能力描述符 JSON；avctl 行为与 Phase 0 完全兼容 |
| 角色 | Tools Engineer |
| 规模 | L |

### WSH-04 实现 Operation Tracker

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-02 |
| 交付物 | 操作跟踪器：operation_id 生成、状态查询、完成回调 |
| 验收标准 | 异步能力（compile.run、eval.run）生成 operation_id；状态可查询；2+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSH-05 实现 core-service HTTP 骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-03 |
| 交付物 | `apps/core-service/` 基于 axum 的 HTTP 服务器 |
| 验收标准 | GET /health 返回 200；GET /capabilities 返回注册的能力列表 JSON；POST /capability/{id} 可路由到对应处理器；启动/停止干净 |
| 角色 | Core Engineer |
| 规模 | M |
| 状态 | done |
| 完成说明 | axum 0.8 HTTP 服务器：GET /health → 200 {"status":"ok"}，GET /capabilities → 21 能力 JSON，POST /capability/{id} → route() 解析 RouteTarget 返回 ResponseEnvelope；workspace 新增 axum/tokio/tower 依赖；4 个集成测试（tower::ServiceExt oneshot） |

### WSH-06 包装 asset/plan/compile 能力面

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-05 |
| 交付物 | asset.ingest、asset.list、plan.validate、plan.submit、compile.run HTTP 处理器 |
| 验收标准 | HTTP 请求与 CLI 产出一致；结构化 envelope 响应；3+ 集成测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSH-07 包装 run/patch/trace/eval/export 能力面

| 字段 | 值 |
| --- | --- |
| 工作流 | H |
| 依赖 | WSH-06 |
| 交付物 | 剩余能力命名空间的 HTTP 处理器 |
| 验收标准 | 全部 12+ 能力可通过 HTTP 调用；所有响应符合 response-envelope schema |
| 角色 | Core Engineer |
| 规模 | M |

---

## 16. Workstream I：MCP 工具面

### WSI-01 定义 MCP tool 与 capability 映射规则

| 字段 | 值 |
| --- | --- |
| 工作流 | I |
| 依赖 | WSH-02 |
| 交付物 | MCP tool 映射表；扩展 `schemas/mcp-tools/av-tool-registry.v0.json` 完整 tool 定义 |
| 验收标准 | 12+ tool 条目有 inputSchema + outputSchema + annotations；fixture 通过 schema 校验 |
| 角色 | Core Engineer |
| 规模 | M |

### WSI-02 实现 mcp-adapter 工具主机骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | I |
| 依赖 | WSI-01, WSH-05 |
| 交付物 | `apps/mcp-adapter/` — MCP stdio/HTTP 工具主机，桥接 core-service |
| 验收标准 | 启动后可响应 tools/list；请求可路由到 core-service 并返回结果 |
| 角色 | Core Engineer |
| 规模 | L |

### WSI-03 实现 system.describe_capabilities 工具

| 字段 | 值 |
| --- | --- |
| 工作流 | I |
| 依赖 | WSI-02 |
| 交付物 | Agent 可通过 MCP 发现全部能力描述符 |
| 验收标准 | 返回 JSON 含 12+ 能力条目 + input/output schema；2+ 测试 |
| 角色 | Tools Engineer |
| 规模 | S |

### WSI-04 MCP 端到端集成测试

| 字段 | 值 |
| --- | --- |
| 工作流 | I |
| 依赖 | WSI-03 |
| 交付物 | E2E 测试：Agent 发现 → 提交 plan → compile → 查询 trace |
| 验收标准 | 至少 3 个 tool 调用组成完整工作流；所有响应可 schema 校验 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 17. Workstream J：灯光运行时骨架

### WSJ-01 扩展灯光 IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | J |
| 依赖 | M6 |
| 交付物 | `crates/ir/` 中 LightingFixture、LightingCue、LightingTopologyNode 类型 |
| 验收标准 | 类型可 serde 往返；与 lighting-topology.v0.json / cue-set.v0.json 对齐 |
| 角色 | Core Engineer |
| 规模 | S |

### WSJ-02 实现 lighting-runtime 事件消费者

| 字段 | 值 |
| --- | --- |
| 工作流 | J |
| 依赖 | WSJ-01 |
| 交付物 | `apps/lighting-runtime/` — 消费 lighting event + timing event 输出 cue ack |
| 验收标准 | 处理 SceneSwitch 产生 "cue_executed" ack；处理 Timing downbeat 产生 "synced" ack；3+ 测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSJ-03 灯光 patch 检查路径

| 字段 | 值 |
| --- | --- |
| 工作流 | J |
| 依赖 | WSJ-02, WSH-02 |
| 交付物 | patch.check 中识别 lighting fixture 引用并做可用性校验 |
| 验收标准 | 缺失 fixture 时产生 PATCH-012 诊断；2+ 测试 |
| 角色 | Core Engineer |
| 规模 | S |
| 状态 | done |
| 完成说明 | check_patch 根据 patch_class 分支：local_content 走音频路径，lighting_cue 走灯光 fixture 校验（PAT-012）；CompiledRevision/PlanBundle 新增 lighting_topology + cue_sets 字段；编译器透传灯光数据；2 个新测试（valid fixture 通过 + missing fixture 产生 PAT-012） |

### WSJ-04 灯光端到端集成测试

| 字段 | 值 |
| --- | --- |
| 工作流 | J |
| 依赖 | WSJ-03 |
| 交付物 | E2E 测试：plan 含灯光 cue → compile → run → lighting-runtime 产出 ack |
| 验收标准 | lighting-acks.json 含至少 1 个 cue_executed + 1 个 synced；artifact 可验证 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 18. Phase 1 里程碑

### M7：能力层抽象建立

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSH-01 ~ WSH-04 全部 done |
| 验收标准 | avctl 通过 capability router 运行；system capabilities 输出 12+ 能力；所有 Phase 0 E2E 测试不降级 |
| 交付物 | capability crate、CapabilityDescriptor schema、avctl 重构、operation tracker |

### M8：服务层可远程调用

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSH-05 ~ WSH-07 + WSI-01 ~ WSI-04 全部 done |
| 验收标准 | HTTP 可调用全部 12+ 能力；MCP Agent 可发现并提交 plan → compile → trace 工作流 |
| 交付物 | core-service HTTP 服务器、mcp-adapter 工具主机、E2E MCP 集成测试 |

### M9：三通道运行时可执行

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSJ-01 ~ WSJ-04 全部 done |
| 验收标准 | audio + visual + lighting 三个运行时均可消费同一 revision 时间线；灯光 ack 可追踪 |
| 交付物 | lighting-runtime、灯光 IR 扩展、E2E 三通道测试 |

### M10：Phase 1 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | M7 + M8 + M9 全部达标 |
| 验收标准 | CLI、HTTP、MCP 三入口执行同一能力语义并产出一致工件；audio + visual + lighting 三运行时可统一调度 |
| 交付物 | Phase 1 验收报告与演示脚本 |

---

## 19. Phase 1 任务依赖图

```text
[M6: Phase 0 Complete]
       │
       ├──── WSH-01 ── WSH-02 ──┬── WSH-03 ── WSH-05 ── WSH-06 ── WSH-07
       │                        ├── WSH-04
       │                        └── WSI-01 ── WSI-02 ── WSI-03 ── WSI-04
       │
       └──── WSJ-01 ── WSJ-02 ── WSJ-03 ── WSJ-04
```

## 20. Phase 1 推荐实施顺序

```text
Round 1 ─── M7: 能力层抽象建立
  │         WSH-01, WSH-02, WSH-04
  │         WSJ-01（可与 WSH-01 并行）
  │
Round 2 ─── WSH-03: avctl 迁移到 capability 路由
  │         WSI-01: MCP tool 映射规则
  │         WSJ-02: lighting-runtime 事件消费者
  │
Round 3 ─── M8: 服务层可远程调用
  │         WSH-05 ~ WSH-07
  │         WSI-02 ~ WSI-04
  │         WSJ-03, WSJ-04
  │
Round 4 ─── M10: Phase 1 验收
              E2E 三入口 + 三通道验收
```

## 21. Phase 1 任务卡总览

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 |
| --- | --- | --- | --- | --- |
| WSH-01 | 定义 Capability 类型与 Schema | H | M | M7 |
| WSH-02 | 实现 capability crate 与能力注册表 | H | M | M7 |
| WSH-03 | 迁移 avctl 到 capability 路由 | H | L | M7 |
| WSH-04 | 实现 Operation Tracker | H | S | M7 |
| WSH-05 | 实现 core-service HTTP 骨架 | H | M | M8 |
| WSH-06 | 包装 asset/plan/compile 能力面 | H | M | M8 |
| WSH-07 | 包装 run/patch/trace/eval/export 能力面 | H | M | M8 |
| WSI-01 | 定义 MCP tool 与 capability 映射规则 | I | M | M8 |
| WSI-02 | 实现 mcp-adapter 工具主机骨架 | I | L | M8 |
| WSI-03 | 实现 system.describe_capabilities 工具 | I | S | M8 |
| WSI-04 | MCP 端到端集成测试 | I | M | M8 |
| WSJ-01 | 扩展灯光 IR 类型 | J | S | M9 |
| WSJ-02 | 实现 lighting-runtime 事件消费者 | J | M | M9 |
| WSJ-03 | 灯光 patch 检查路径 | J | S | M9 |
| WSJ-04 | 灯光端到端集成测试 | J | M | M9 |

Phase 1 共计 15 张任务卡，覆盖 3 个 Workstream 和 4 个里程碑。全部 done（2026-04-03）。

---

# Phase 2：演出系统化

## 22. Phase 2 目标

Phase 1 证明了 CLI / HTTP / MCP 三入口语义等价和 audio / visual / lighting 三运行时统一调度。

Phase 2 的目标是：

1. 在 Core 中固定统一适配插件协议边界（Adapter Plugin Contract），使后续接入任意硬件后端只需实现一个 adapter plugin。
2. 固定外部资源 HUB 描述对象，使音频素材、GLSL 场景、灯光 preset 等资源以标准化引用方式被 Compiler / Runtime 消费。
3. 定义分布式装置部署层的正式对象（node / deployment / transport），为后续多节点编排提供稳定建模基础。
4. 引入运行时健康监控与降级策略骨架，为长时运行稳定性做基础建设。
5. 完善 revision 生命周期 HTTP handler（publish / archive），补全 Phase 1 遗留 stub。

Phase 2 明确不做的事：

- 不做真实硬件驱动实现（由独立 adapter 仓库承接）
- 不做消息总线完整实现（只定义 TransportContract 对象）
- 不做 GUI 产品
- 不做 MIDI / OSC 外部控制器实时桥接（Phase 3）
- 不做完整权限模型（仅声明授权占位）

## 23. Workstream K：统一适配插件协议

### WSK-01 定义 AdapterPluginManifest IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | K |
| 依赖 | M10 (Phase 1 complete) |
| 交付物 | `crates/ir/` 中 AdapterPluginManifest、HealthContract IR 类型 |
| 验收标准 | 类型可 serde 往返；与 schemas/capability/adapter-plugin-manifest.v0.json 字段对齐；1+ serde round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSK-02 定义 ResourceHubDescriptor IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | K |
| 依赖 | M10 |
| 交付物 | `crates/ir/` 中 ResourceHubDescriptor、HubCompatibility IR 类型 |
| 验收标准 | 类型可 serde 往返；与 schemas/asset/resource-hub-descriptor.v0.json 字段对齐；1+ serde round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSK-03 实现 adapter-registry crate 骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | K |
| 依赖 | WSK-01 |
| 交付物 | `crates/adapter-registry/` — 插件注册表、按 backend_kind 查询、健康状态汇总 |
| 验收标准 | register/lookup/list/health_summary API；3+ 单元测试；未知 plugin_id 返回结构化错误 |
| 角色 | Core Engineer |
| 规模 | M |

### WSK-04 实现 resource-hub crate 骨架

| 字段 | 值 |
| --- | --- |
| 工作流 | K |
| 依赖 | WSK-02 |
| 交付物 | `crates/resource-hub/` — HUB 注册表、按 resource_kind 查询、资源引用解析 |
| 验收标准 | register_hub/resolve_resource/list_hubs API；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSK-05 集成 adapter/hub 到 avctl 与 core-service

| 字段 | 值 |
| --- | --- |
| 工作流 | K |
| 依赖 | WSK-03, WSK-04 |
| 交付物 | `avctl system adapters/hubs` 命令；`core-service` 新增 adapter.list / hub.list 能力面 |
| 验收标准 | CLI 输出已注册 adapter 与 hub 列表；HTTP POST /capability/{adapter.list,hub.list} 可用；所有现有 E2E 不降级 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 24. Workstream L：分布式部署对象建模

### WSL-01 定义分布式部署 Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | L |
| 依赖 | M10 |
| 交付物 | `schemas/deployment/distributed-node-descriptor.v0.json`、`schemas/deployment/deployment-profile.v0.json`、`schemas/deployment/transport-contract.v0.json` |
| 验收标准 | 三个 Schema 均有 valid + invalid fixture 通过校验；字段与 31 号文档对齐 |
| 角色 | Core Engineer |
| 规模 | M |

### WSL-02 定义 Deployment IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | L |
| 依赖 | WSL-01 |
| 交付物 | `crates/ir/` 中 DistributedNodeDescriptor、DeploymentProfile、TransportContract 类型 |
| 验收标准 | 类型可 serde 往返；与 WSL-01 Schema 对齐；3 个 serde round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSL-03 部署验证器

| 字段 | 值 |
| --- | --- |
| 工作流 | L |
| 依赖 | WSL-02 |
| 交付物 | `crates/validator/` 新增 deployment 校验规则（DEP-001 ~ DEP-003） |
| 验收标准 | 校验孤立节点（无 transport）、传输延迟预算溢出、节点 ID 冲突；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | S |

---

## 25. Workstream M：运行时健康与降级

### WSM-01 定义 HealthSnapshot IR 类型与 Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | M |
| 依赖 | WSK-01 |
| 交付物 | `crates/ir/` 中 BackendHealthSnapshot、DegradeMode 类型；`schemas/runtime/health-snapshot.v0.json` |
| 验收标准 | 类型可 serde 往返；schema fixture 通过校验；1+ serde round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSM-02 实现 health-monitor 模块

| 字段 | 值 |
| --- | --- |
| 工作流 | M |
| 依赖 | WSM-01, WSK-03 |
| 交付物 | `crates/scheduler/` 新增 health_monitor 模块：collect_health → aggregate → degrade_decision |
| 验收标准 | 输入多个 BackendHealthSnapshot → 输出 DegradeDecision；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSM-03 降级 trace 集成

| 字段 | 值 |
| --- | --- |
| 工作流 | M |
| 依赖 | WSM-02 |
| 交付物 | degrade 事件写入 trace JSONL；avctl trace events 可查看降级记录 |
| 验收标准 | 降级事件出现在 trace 事件流中；E2E 可验证 |
| 角色 | Core Engineer |
| 规模 | S |

---

## 26. Workstream N：Phase 2 收尾与验收

### WSN-01 完善 revision publish/archive HTTP handler

| 字段 | 值 |
| --- | --- |
| 工作流 | N |
| 依赖 | M10 |
| 交付物 | core-service revision.publish / revision.archive handler 真实实现 |
| 验收标准 | HTTP POST 可变更 revision 状态；对应 Rust 测试；mcp-adapter 相应 tool 可调用 |
| 角色 | Core Engineer |
| 规模 | S |

### WSN-02 Phase 2 端到端验收

| 字段 | 值 |
| --- | --- |
| 工作流 | N |
| 依赖 | WSK-05, WSL-03, WSM-03, WSN-01 |
| 交付物 | `tests/e2e/phase2_acceptance.sh`；Phase 2 验收报告 |
| 验收标准 | adapter + hub 注册 → 部署 profile 校验 → 健康采集 → 降级决策 → trace 可查；所有三入口一致 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 27. Phase 2 里程碑

### M11：适配插件与资源 HUB 边界建立

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSK-01 ~ WSK-05 全部 done |
| 验收标准 | AdapterPluginManifest / ResourceHubDescriptor IR 类型与 Schema 对齐；adapter-registry + resource-hub crate 可用；avctl system adapters/hubs 输出；core-service 新增能力面 |
| 交付物 | adapter-registry crate、resource-hub crate、CLI 与 HTTP 扩展 |

### M12：分布式部署对象建模完成

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSL-01 ~ WSL-03 全部 done |
| 验收标准 | 三类部署对象 Schema + IR 类型 + 基础校验规则就绪；fixture 覆盖 |
| 交付物 | deployment/ Schema 分类、IR 类型、validator 扩展 |

### M13：运行时健康与降级骨架

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSM-01 ~ WSM-03 全部 done |
| 验收标准 | HealthSnapshot 类型 + Schema 就绪；health_monitor 模块可做降级决策；降级事件进入 trace |
| 交付物 | health-snapshot Schema、health_monitor 模块、trace 集成 |

### M14：Phase 2 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | M11 + M12 + M13 + WSN-01 全部达标 |
| 验收标准 | adapter/hub/deployment/health 全链路可验证；revision lifecycle HTTP 完整；所有 E2E 通过 |
| 交付物 | Phase 2 验收报告与演示脚本 |

---

## 28. Phase 2 任务依赖图

```text
[M10: Phase 1 Complete]
       │
       ├──── WSK-01 ── WSK-03 ── WSK-05
       │     WSK-02 ── WSK-04 ──┘
       │              WSM-01 ── WSM-02 ── WSM-03
       │
       ├──── WSL-01 ── WSL-02 ── WSL-03
       │
       └──── WSN-01
                      └── WSN-02 (依赖 WSK-05, WSL-03, WSM-03, WSN-01)
```

## 29. Phase 2 推荐实施顺序

```text
Round 1 ─── Schema + IR 类型基础
  │         WSK-01, WSK-02, WSL-01, WSM-01, WSN-01
  │
Round 2 ─── Crate 实现
  │         WSK-03, WSK-04, WSL-02, WSL-03, WSM-02
  │
Round 3 ─── 集成与收尾
  │         WSK-05, WSM-03, WSN-02
  │
Round 4 ─── M14: Phase 2 验收
```

## 30. Phase 2 任务卡总览

> **Phase 2 状态：全部完成** — 13/13 卡 Done，M14 验收通过（2026-04-04）

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 | 状态 |
| --- | --- | --- | --- | --- | --- |
| WSK-01 | 定义 AdapterPluginManifest IR 类型 | K | S | M11 | ✅ Done |
| WSK-02 | 定义 ResourceHubDescriptor IR 类型 | K | S | M11 | ✅ Done |
| WSK-03 | 实现 adapter-registry crate 骨架 | K | M | M11 | ✅ Done |
| WSK-04 | 实现 resource-hub crate 骨架 | K | M | M11 | ✅ Done |
| WSK-05 | 集成 adapter/hub 到 avctl 与 core-service | K | M | M11 | ✅ Done |
| WSL-01 | 定义分布式部署 Schema | L | M | M12 | ✅ Done |
| WSL-02 | 定义 Deployment IR 类型 | L | S | M12 | ✅ Done |
| WSL-03 | 部署验证器 | L | S | M12 | ✅ Done |
| WSM-01 | 定义 HealthSnapshot IR 类型与 Schema | M | S | M13 | ✅ Done |
| WSM-02 | 实现 health-monitor 模块 | M | M | M13 | ✅ Done |
| WSM-03 | 降级 trace 集成 | M | S | M13 | ✅ Done |
| WSN-01 | 完善 revision publish/archive HTTP handler | N | S | M14 | ✅ Done |
| WSN-02 | Phase 2 端到端验收 | N | M | M14 | ✅ Done |

Phase 2 共计 13 张任务卡，覆盖 4 个 Workstream 和 4 个里程碑。全部完成。

---

# Phase 3：统一适配协议与后端操作化

## 31. Phase 3 目标

Phase 2 建立了适配插件注册表、资源 HUB 注册表、分布式部署对象模型与健康决策骨架。

Phase 3 的目标是：

1. 把 30 号文档 §4.2 定义的 7 方法统一插件协议形式化为 Rust trait，使三类后端共享同一协议边界。
2. 实现适配插件生命周期管理（加载、就绪检查、停机），脱离纯内存桩模式。
3. 为资源 HUB 增加 SQLite 持久化和资源解析缓存，使注册数据跨运行保持。
4. 实现最小授权策略引擎，使 capability 请求可被策略拒绝或降级。
5. 基于 trait 实现三类 Reference Backend Adapter（audio / visual / lighting），替换现有 FakeBackendClient。
6. 在 avctl / core-service / mcp-adapter 三入口完成新能力集成与端到端验证。

Phase 3 明确不做的事：

- 不做真实硬件驱动（ALSA、CoreAudio、DMX、Art-Net）绑定——仅参考实现
- 不做多节点分布式编排运行时（Doc 31 §7 结论：独立仓库）
- 不做 GUI 或 Web 前端
- 不做资源 HUB 市场 / CDN 分发

## 32. Workstream O：统一适配协议 Trait

### WSO-01 定义 BackendAdapter trait

| 字段 | 值 |
| --- | --- |
| 工作流 | O |
| 依赖 | M14 (Phase 2 complete) |
| 交付物 | `crates/ir/src/` 中 BackendAdapter trait 定义（7 方法：describe_backend, prepare_backend, apply_show_state, execute_payload, collect_backend_status, apply_degrade_mode, shutdown_backend） |
| 验收标准 | trait 可编译；方法签名覆盖 30 号文档 §4.2 全部语义；3+ 文档测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSO-02 定义 ExecutablePayload 与 BackendTopology 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | O |
| 依赖 | WSO-01 |
| 交付物 | `crates/ir/src/` 中 ExecutablePayload（音频 / 视觉 / 灯光三种变体）、BackendTopology 类型；对应 JSON Schema |
| 验收标准 | 类型可 serde 往返；schema fixture 通过校验；覆盖三类后端差异化 payload |
| 角色 | Core Engineer |
| 规模 | M |

### WSO-03 实现 NullBackendAdapter 参考实现

| 字段 | 值 |
| --- | --- |
| 工作流 | O |
| 依赖 | WSO-01, WSO-02 |
| 交付物 | `crates/ir/src/` 或 `crates/scheduler/src/` 中 NullBackendAdapter 结构体，impl BackendAdapter |
| 验收标准 | 所有 7 方法有默认行为（no-op 或 log）；可替代现有 FakeBackendClient 用于测试；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | S |

---

## 33. Workstream P：适配插件生命周期与 HUB 持久化

### WSP-01 实现 AdapterPluginLoader

| 字段 | 值 |
| --- | --- |
| 工作流 | P |
| 依赖 | WSO-01, WSK-03 |
| 交付物 | `crates/adapter-registry/src/loader.rs` — 从 manifest 列表加载并实例化 BackendAdapter trait objects |
| 验收标准 | 从 JSON manifest 列表加载 → 注册 → 就绪检查 → 返回 trait object 集合；错误路径有结构化 diagnostic；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSP-02 实现 adapter-registry SQLite 持久化

| 字段 | 值 |
| --- | --- |
| 工作流 | P |
| 依赖 | WSP-01 |
| 交付物 | `crates/adapter-registry/src/persistence.rs` — AdapterPluginManifest 写入/查询 SQLite |
| 验收标准 | register 后重启可恢复注册表状态；list / lookup 从 SQLite 查询；migration DDL 在 storage crate 或 init 脚本中；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSP-03 实现 resource-hub SQLite 持久化

| 字段 | 值 |
| --- | --- |
| 工作流 | P |
| 依赖 | WSK-04 |
| 交付物 | `crates/resource-hub/src/persistence.rs` — ResourceHubDescriptor 写入/查询 SQLite |
| 验收标准 | register_hub 后重启可恢复；resolve_resource 从持久化数据查询；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSP-04 实现 ResourceResolver 缓存层

| 字段 | 值 |
| --- | --- |
| 工作流 | P |
| 依赖 | WSP-03 |
| 交付物 | `crates/resource-hub/src/resolver.rs` — 带 LRU 缓存的资源引用解析 |
| 验收标准 | 首次 resolve 走 SQLite，后续命中缓存；cache_stats 可查询命中率；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | S |

---

## 34. Workstream Q：授权策略与 Reference Backend

### WSQ-01 定义 AuthorizationPolicy 类型与 Schema

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | M14 |
| 交付物 | `crates/ir/src/` 中 AuthorizationPolicy、PolicyRule 类型；`schemas/capability/authorization-policy.v0.json` |
| 验收标准 | 类型覆盖 08 号文档 §4 三种角色（human_operator, external_agent, auto_recovery）的权限范围；schema fixture 通过校验 |
| 角色 | Core Engineer |
| 规模 | S |

### WSQ-02 实现 PolicyEngine 最小授权执行

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | WSQ-01 |
| 交付物 | `crates/capability/src/policy.rs` — PolicyEngine 结构体，evaluate(request, actor_context) → allow / deny / degrade |
| 验收标准 | human_operator 全部允许；external_agent 阻止 emergency patch；auto_recovery 仅允许 degrade/rollback；5+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSQ-03 集成 PolicyEngine 到 capability route

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | WSQ-02 |
| 交付物 | capability router 在 dispatch 前调用 policy_engine.evaluate()；被拒绝时返回带 diagnostic 的 ResponseEnvelope |
| 验收标准 | 未授权请求返回 error envelope + AUZ-001 diagnostic；已授权请求正常执行；E2E 演示 deny 场景；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSQ-04 实现 AudioReferenceBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | WSO-03 |
| 交付物 | `crates/scheduler/src/audio_backend.rs` — impl BackendAdapter for AudioReferenceBackend |
| 验收标准 | describe_backend 返回 audio 类型描述；execute_payload 消费 AudioPayload 变体并写日志；collect_backend_status 返回 healthy snapshot；替换现有 audio_client 中的 FakeBackendClient；4+ 测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSQ-05 实现 VisualReferenceBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | WSO-03 |
| 交付物 | `apps/visual-runtime/` 中 impl BackendAdapter for VisualReferenceBackend |
| 验收标准 | describe_backend 返回 visual 类型描述；execute_payload 消费 VisualPayload 变体并写日志；replace FakeBackendClient；4+ 测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSQ-06 实现 LightingReferenceBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | Q |
| 依赖 | WSO-03 |
| 交付物 | `apps/lighting-runtime/` 中 impl BackendAdapter for LightingReferenceBackend |
| 验收标准 | describe_backend 返回 lighting 类型描述；execute_payload 消费 LightingPayload 变体并写日志；replace FakeBackendClient；4+ 测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

---

## 35. Workstream R：Phase 3 集成与验收

### WSR-01 集成 adapter lifecycle 到 avctl/core-service/mcp-adapter

| 字段 | 值 |
| --- | --- |
| 工作流 | R |
| 依赖 | WSP-02, WSQ-03 |
| 交付物 | avctl system adapters 显示持久化数据；core-service adapter.load / adapter.shutdown handler；mcp-adapter 对应 tool |
| 验收标准 | load → register → list → shutdown 全链路可通过三入口；adapter 状态持久化跨重启验证；能力数 23→26+ |
| 角色 | Tools Engineer |
| 规模 | M |

### WSR-02 集成 resource-hub 持久化到三入口

| 字段 | 值 |
| --- | --- |
| 工作流 | R |
| 依赖 | WSP-04, WSQ-03 |
| 交付物 | avctl system hubs 显示持久化数据；core-service hub.register / hub.resolve handler；mcp-adapter 对应 tool |
| 验收标准 | register → resolve → list 全链路可通过三入口；持久化跨重启验证 |
| 角色 | Tools Engineer |
| 规模 | M |

### WSR-03 集成 Reference Backends 到 scheduler run 链路

| 字段 | 值 |
| --- | --- |
| 工作流 | R |
| 依赖 | WSQ-04, WSQ-05, WSQ-06 |
| 交付物 | `crates/scheduler/` 与 runtime apps 使用 BackendAdapter trait 替代 FakeBackendClient；avctl run start 可选择 reference backend |
| 验收标准 | avctl run start 使用 reference backend 时输出 adapter 日志；trace 记录包含 adapter 执行事件；现有 E2E 仍绿 |
| 角色 | Core Engineer |
| 规模 | L |

### WSR-04 Phase 3 端到端验收

| 字段 | 值 |
| --- | --- |
| 工作流 | R |
| 依赖 | WSR-01, WSR-02, WSR-03 |
| 交付物 | `tests/e2e/phase3_acceptance.sh`；Phase 3 验收报告 |
| 验收标准 | adapter load → register → prepare → run → collect_status → shutdown → 持久化恢复；hub register → resolve → 缓存命中；authorization deny 场景；全部三入口一致；trace 审计链完整 |
| 角色 | Tools Engineer |
| 规模 | L |

---

## 36. Phase 3 里程碑

### M15：统一适配协议固定

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSO-01 ~ WSO-03 全部 done |
| 验收标准 | BackendAdapter trait 7 方法已定义并可编译；ExecutablePayload / BackendTopology 类型与 schema 对齐；NullBackendAdapter 可替代 FakeBackendClient |
| 交付物 | BackendAdapter trait、ExecutablePayload 类型、NullBackendAdapter 参考实现 |

### M16：插件生命周期与 HUB 持久化

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSP-01 ~ WSP-04 全部 done |
| 验收标准 | adapter 从 manifest 加载可用；adapter-registry 与 resource-hub 数据 SQLite 持久化且跨重启恢复；资源解析带缓存 |
| 交付物 | AdapterPluginLoader、adapter + hub SQLite 持久化层、ResourceResolver 缓存 |

### M17：授权执行与三类 Reference Backend

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSQ-01 ~ WSQ-06 全部 done |
| 验收标准 | PolicyEngine 可 evaluate 三种 actor 权限；capability route 集成授权拦截；Audio/Visual/Lighting Reference Backend impl BackendAdapter trait |
| 交付物 | AuthorizationPolicy 类型与 schema、PolicyEngine、三类 ReferenceBackend |

### M18：Phase 3 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | M15 + M16 + M17 + WSR-01 ~ WSR-04 全部达标 |
| 验收标准 | 统一协议 trait 贯穿三类后端；adapter 生命周期与 hub 持久化可验证；授权执行拦截可演示；E2E 全链路通过；trace 证据链完整 |
| 交付物 | Phase 3 验收报告与演示脚本 |

---

## 37. Phase 3 任务依赖图

```text
[M14: Phase 2 Complete]
       │
       ├──── WSO-01 ── WSO-02 ── WSO-03
       │                          │
       │     WSP-01 ──────────────┤
       │       │                  │
       │     WSP-02               │
       │                          │
       │     WSP-03 ── WSP-04     │
       │                          │
       │     WSQ-01 ── WSQ-02 ── WSQ-03
       │                          │
       │     WSQ-04 ──────────────┤
       │     WSQ-05 ──────────────┤
       │     WSQ-06 ──────────────┤
       │                          │
       │     WSR-01 ──────────────┤
       │     WSR-02 ──────────────┤
       │     WSR-03 ──────────────┤
       │                          │
       └──── WSR-04 ◄─────────── (WSR-01, WSR-02, WSR-03)
```

## 38. Phase 3 推荐实施顺序

```text
Round 1 ─── 统一协议 Trait + 授权类型 + HUB 持久化基础
  │         WSO-01, WSO-02, WSQ-01, WSP-03
  │
Round 2 ─── Trait 实现 + 生命周期 + 策略引擎
  │         WSO-03, WSP-01, WSP-02, WSP-04, WSQ-02
  │
Round 3 ─── Reference Backend + 授权集成
  │         WSQ-03, WSQ-04, WSQ-05, WSQ-06
  │
Round 4 ─── 三入口集成与验收
  │         WSR-01, WSR-02, WSR-03, WSR-04
```

## 39. Phase 3 任务卡总览

> **Phase 3 状态：全部完成** — 17/17 卡 Done，M18 验收通过（2026-04-04）

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 | 状态 |
| --- | --- | --- | --- | --- | --- |
| WSO-01 | 定义 BackendAdapter trait | O | M | M15 | ✅ Done |
| WSO-02 | 定义 ExecutablePayload 与 BackendTopology 类型 | O | M | M15 | ✅ Done |
| WSO-03 | 实现 NullBackendAdapter 参考实现 | O | S | M15 | ✅ Done |
| WSP-01 | 实现 AdapterPluginLoader | P | M | M16 | ✅ Done |
| WSP-02 | 实现 adapter-registry SQLite 持久化 | P | M | M16 | ✅ Done |
| WSP-03 | 实现 resource-hub SQLite 持久化 | P | M | M16 | ✅ Done |
| WSP-04 | 实现 ResourceResolver 缓存层 | P | S | M16 | ✅ Done |
| WSQ-01 | 定义 AuthorizationPolicy 类型与 Schema | Q | S | M17 | ✅ Done |
| WSQ-02 | 实现 PolicyEngine 最小授权执行 | Q | M | M17 | ✅ Done |
| WSQ-03 | 集成 PolicyEngine 到 capability route | Q | M | M17 | ✅ Done |
| WSQ-04 | 实现 AudioReferenceBackend | Q | M | M17 | ✅ Done |
| WSQ-05 | 实现 VisualReferenceBackend | Q | M | M17 | ✅ Done |
| WSQ-06 | 实现 LightingReferenceBackend | Q | M | M17 | ✅ Done |
| WSR-01 | 集成 adapter lifecycle 到三入口 | R | M | M18 | ✅ Done |
| WSR-02 | 集成 resource-hub 持久化到三入口 | R | M | M18 | ✅ Done |
| WSR-03 | 集成 Reference Backends 到 scheduler run | R | L | M18 | ✅ Done |
| WSR-04 | Phase 3 端到端验收 | R | L | M18 | ✅ Done |

Phase 3 共计 17 张任务卡，覆盖 4 个 Workstream（O/P/Q/R）和 4 个里程碑（M15-M18）。全部完成。

---

# Phase 4：演出闭环与可演示样例

## 40. Phase 4 目标

Phase 3 完成了统一适配协议 trait、适配器与资源 HUB 持久化、最小授权策略引擎和三类 Reference Backend 的集成验收。

Phase 4 的目标是：

1. 补齐补丁回退完整闭环：资源释放、多后端状态恢复、回退后一致性验证（审计报告 P1 收口）。
2. 拆分 storage crate 为三个聚焦子模块，降低后续演进复杂度（审计报告建议）。
3. 引入外部控制事件（MIDI CC/Note、OSC 消息）的 Schema + IR + Scheduler 消费路径，完成产品路线图 Phase 2 §11.3 承诺的外部控制器对接。
4. 建立演出模板与场景包的 IR 类型与生命周期管理，使 show 可被打包、分享和复用。
5. 以示范性第三方音频分析器和视觉执行器证明 adapter-registry 生态可行性。
6. 引入长运行压力测试与故障注入框架，验证降级决策在 N-bar 连续调度下的正确性。
7. 构建内置 Showcase 样例包——类似 Sonic Pi 的 examples，一条命令即可演示完整视听联动流程，让用户直观感受系统最终效果。

Phase 4 明确不做的事：

- 不做真实硬件驱动绑定（ALSA、CoreAudio、DMX、Art-Net）
- 不做多节点分布式编排运行时
- 不做 GUI 或 Web 前端
- 不做 Ableton Link 时钟同步（仅 MIDI/OSC 事件桥接）
- 不做资源 HUB 市场 / CDN 分发

## 41. Workstream S：契约加固与工程债收口

### WSS-01 补丁回退资源释放与状态恢复类型

| 字段 | 值 |
| --- | --- |
| 工作流 | S |
| 依赖 | M18 (Phase 3 complete) |
| 交付物 | `crates/ir/` 新增 RollbackCheckpoint（resource_handles、backend_snapshots、show_state_snapshot）和 ResourceHandleState 枚举（Active / Released / Warming） |
| 验收标准 | 类型可 serde 往返；与 08 号文档 §4 回退策略语义对齐；2+ serde round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSS-02 storage crate 拆分为三子模块

| 字段 | 值 |
| --- | --- |
| 工作流 | S |
| 依赖 | M18 |
| 交付物 | `crates/storage/` 内部拆分为 `artifact_layout`（目录布局 + 文件落盘）、`asset_ingest`（接入 + 分析缓存）、`query_registry`（注册表查询 + SQLite 索引）三个子模块；对外 pub API 不变 |
| 验收标准 | 现有 storage 单元测试全部不降级；各子模块独立可测；依赖方无需改 use 路径 |
| 角色 | Core Engineer |
| 规模 | M |

### WSS-03 Schema 扩展点标注与裸对象收口

| 字段 | 值 |
| --- | --- |
| 工作流 | S |
| 依赖 | M18 |
| 交付物 | `schemas/` 中所有 IR Schema 的故意开放字段添加 `x-stability: experimental` 注解；`asset-ir`、`structure-ir`、`timeline-entry`、`visual-ir` 裸对象字段增补 `minProperties` 或结构化约束 |
| 验收标准 | `scripts/schema_validate.py` 可检测 `x-stability` 注解存在性；现有 fixture 不降级；新增 1 个校验规则测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSS-04 补丁回退完整闭环 E2E 测试

| 字段 | 值 |
| --- | --- |
| 工作流 | S |
| 依赖 | WSS-01 |
| 交付物 | `crates/patch-manager/` 新增 rollback_with_checkpoint() → 生成 RollbackCheckpoint → 释放旧资源句柄 → 恢复 show_state；`tests/e2e/` 新增 multi-backend patch → rollback → checkpoint 校验脚本 |
| 验收标准 | 回退后 RollbackCheckpoint 写入 trace；旧 revision 资源句柄标记 Released；4+ 单元测试 + 1 E2E 脚本 |
| 角色 | Core Engineer |
| 规模 | M |

---

## 42. Workstream T：外部控制 MVP

### WST-01 定义 ExternalControl 事件 Schema 与 IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | T |
| 依赖 | M18 |
| 交付物 | `schemas/runtime/external-control-event.v0.json`（MidiCC、MidiNote、OscMessage 三种 payload）；`crates/ir/` 新增 ExternalControlEvent 枚举与子类型；RuntimePayload 新增 ExternalControl 变体 |
| 验收标准 | Schema 有 valid + invalid fixture（3+3）；IR 类型可 serde 往返；2+ round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WST-02 ExternalControlAdapter trait 与 NullControlAdapter

| 字段 | 值 |
| --- | --- |
| 工作流 | T |
| 依赖 | WST-01 |
| 交付物 | `crates/ir/` 新增 ExternalControlAdapter trait（bind_source / unbind_source / poll_events / list_bindings）；`crates/scheduler/` 新增 NullControlAdapter 参考实现 |
| 验收标准 | trait 可编译；NullControlAdapter 实现全部方法；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WST-03 Scheduler 消费外部控制事件

| 字段 | 值 |
| --- | --- |
| 工作流 | T |
| 依赖 | WST-02 |
| 交付物 | `crates/scheduler/` 扩展 run 流程：每 tick 轮询 ExternalControlAdapter → 将 MIDI/OSC 映射到 SemanticEvent 或 PatchProposal → 记入 trace |
| 验收标准 | MIDI CC fixture → scheduler run → 输出对应 SemanticEvent；OSC fixture → scheduler run → 输出对应 SemanticEvent；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | L |

### WST-04 CLI 与 MCP 新增 control 命名空间

| 字段 | 值 |
| --- | --- |
| 工作流 | T |
| 依赖 | WST-03 |
| 交付物 | 注册 control.bind / control.unbind / control.list / control.status 四个能力；avctl、core-service、mcp-adapter 三入口全部接入 |
| 验收标准 | `avctl control list` 输出已绑定源列表；HTTP POST /capability/control.bind 可用；MCP tools_call control.bind 可用；总能力数 33 |
| 角色 | Tools Engineer |
| 规模 | M |

### WST-05 外部控制 E2E 测试

| 字段 | 值 |
| --- | --- |
| 工作流 | T |
| 依赖 | WST-04 |
| 交付物 | `tests/fixtures/controls/` 新增 MIDI CC + OSC 控制 fixture 文件；`tests/e2e/external_control_smoke.sh` 完整流程：bind → 注入控制事件 → scheduler run → trace 验证 |
| 验收标准 | E2E 脚本 10+ 断言全部通过；fixture 加载 → 事件映射 → trace 记录端到端可验证 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 43. Workstream U：演出模板与场景包

### WSU-01 定义 ShowTemplate 与 ScenePack Schema 和 IR 类型

| 字段 | 值 |
| --- | --- |
| 工作流 | U |
| 依赖 | M18 |
| 交付物 | `schemas/planning/show-template.v0.json`（模板元数据 + 段落引用 + 默认参数）；`schemas/planning/scene-pack.v0.json`（场景列表 + 资源清单 + 过渡策略）；`crates/ir/` 新增 ShowTemplate、ScenePack、SceneDescriptor 类型 |
| 验收标准 | 两个 Schema 各有 valid + invalid fixture（2+1）；IR 类型 serde 往返；3+ round-trip 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSU-02 模板注册表与场景包加载器

| 字段 | 值 |
| --- | --- |
| 工作流 | U |
| 依赖 | WSU-01 |
| 交付物 | `crates/storage/` 新增 template_registry 模块：register_template / list_templates / load_template / resolve_scene_pack API |
| 验收标准 | 注册 → 列出 → 加载 → 解析场景包端到端可用；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSU-03 场景切换运行时集成

| 字段 | 值 |
| --- | --- |
| 工作流 | U |
| 依赖 | WSU-02, WST-03 |
| 交付物 | `crates/scheduler/` 新增 scene_manager 模块：activate_scene / list_active_scenes / transition（cross-fade 语义在 show_state 中以 transition_progress 字段表示）；`crates/trace/` 记录 SceneTransitionEvent |
| 验收标准 | 加载场景包 → activate scene A → transition to scene B → trace 记录两次事件；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSU-04 CLI 与 MCP 新增 template / scene 命名空间

| 字段 | 值 |
| --- | --- |
| 工作流 | U |
| 依赖 | WSU-03 |
| 交付物 | 注册 template.list / template.load / scene.list / scene.activate 四个能力；avctl、core-service、mcp-adapter 三入口全部接入 |
| 验收标准 | `avctl template list` 输出已注册模板；`avctl scene activate <name>` 触发场景切换；总能力数 37；所有现有 E2E 不降级 |
| 角色 | Tools Engineer |
| 规模 | M |

---

## 44. Workstream V：第三方适配示例

### WSV-01 示范性第三方音频分析器 Adapter

| 字段 | 值 |
| --- | --- |
| 工作流 | V |
| 依赖 | M18 |
| 交付物 | `tests/fixtures/adapters/example-audio-analyzer/` 包含 adapter manifest + 分析逻辑 stub；`crates/adapter-registry/` 中 AdapterPluginLoader 可加载该示例 adapter 并调用其 analyze() 方法 |
| 验收标准 | 加载 → 就绪检查 → 调用分析 → 返回结构化结果 → 注册表可查；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSV-02 示范性第三方视觉执行器 Adapter

| 字段 | 值 |
| --- | --- |
| 工作流 | V |
| 依赖 | WSV-01 |
| 交付物 | `tests/fixtures/adapters/example-visual-executor/` 包含 adapter manifest + 渲染 stub；AdapterPluginLoader 可加载并通过 BackendAdapter trait dispatch visual payloads |
| 验收标准 | 加载 → prepare → execute_payload(Visual) → 返回 ack → 注册表可查；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSV-03 第三方 Adapter 集成 E2E 测试

| 字段 | 值 |
| --- | --- |
| 工作流 | V |
| 依赖 | WSV-02 |
| 交付物 | `tests/e2e/adapter_ecosystem_smoke.sh`：加载两个示范 adapter → avctl adapter list 确认注册 → scheduler run 使用外部 visual executor → trace 验证 |
| 验收标准 | E2E 脚本 8+ 断言全部通过 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 45. Workstream W：运行稳定性加固

### WSW-01 长运行压力测试 Harness

| 字段 | 值 |
| --- | --- |
| 工作流 | W |
| 依赖 | M18 |
| 交付物 | `tests/stress/` 新增 long_run_harness.rs：生成 N-bar（N=128+）timeline → scheduler run → 验证无 panic、无 event 丢失、clock drift ≤ 1ms |
| 验收标准 | 128-bar 压力测试通过；事件计数与 timeline 预期一致；内存不持续增长（采样比较首尾 RSS）；2+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSW-02 故障注入与降级恢复测试

| 字段 | 值 |
| --- | --- |
| 工作流 | W |
| 依赖 | WSW-01 |
| 交付物 | `crates/scheduler/` 新增 fault_injection 模块：FaultInjector trait + NullFaultInjector + FailAtBarInjector（在指定 bar 使后端返回错误）；Scheduler run 接受可选 FaultInjector |
| 验收标准 | 注入故障 → health_monitor degrade_decision 被触发 → DegradeEvent 写入 trace → scheduler 继续执行不 panic；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSW-03 回归测试套件与 bench 基线

| 字段 | 值 |
| --- | --- |
| 工作流 | W |
| 依赖 | WSW-02 |
| 交付物 | `vidodo-src/benches/` 新增 scheduler_bench.rs（compile + 64-bar run 基准）；`tests/e2e/regression_suite.sh` 汇总运行 Phase 0~4 全部 E2E 脚本 |
| 验收标准 | `cargo bench --workspace` 输出 scheduler 基线数据；regression_suite.sh 零失败；bench 结果写入 `artifacts/analysis/reports/bench-baseline.json` |
| 角色 | Core Engineer |
| 规模 | M |

---

## 46. Workstream X：内置 Showcase 样例

### WSX-01 定义样例包格式与首个样例资产集

| 字段 | 值 |
| --- | --- |
| 工作流 | X |
| 依赖 | WSU-01, WST-01 |
| 交付物 | `examples/` 目录规范：每个样例为独立子目录，包含 `vidodo-asset-pack.json` + 音频素材 + `set-plan.json` + `scene-pack.json`（可选）；首个样例 `examples/minimal-beat-show/`：一段节拍循环 + 视觉脉冲 + 灯光同步 |
| 验收标准 | `examples/minimal-beat-show/` 包含完整文件集；`avctl plan validate examples/minimal-beat-show/set-plan.json` 通过；资产可 ingest |
| 角色 | Content Engineer |
| 规模 | M |

### WSX-02 构建 "Ambient Drift" 多段落样例

| 字段 | 值 |
| --- | --- |
| 工作流 | X |
| 依赖 | WSX-01 |
| 交付物 | `examples/ambient-drift/`：3 个段落（intro → body → outro），多素材叠加，用视觉过渡 + 灯光渐变展示段落切换效果 |
| 验收标准 | plan validate + compile + run 完整通过；trace 中可观察 section transition 事件；scene-pack 中至少 2 个场景 |
| 角色 | Content Engineer |
| 规模 | M |

### WSX-03 构建 "Live Patch Demo" 交互样例

| 字段 | 值 |
| --- | --- |
| 工作流 | X |
| 依赖 | WSX-01, WSS-04 |
| 交付物 | `examples/live-patch-demo/`：运行中提交 patch → 可视化 patch 窗口 → 回退 → 恢复；包含预编排的 patch proposal fixture |
| 验收标准 | avctl run → avctl patch apply → avctl patch rollback 完整流程可演示；trace 记录 patch + rollback 事件；RollbackCheckpoint 可验证 |
| 角色 | Content Engineer |
| 规模 | M |

### WSX-04 avctl demo 命令与一键演示运行器

| 字段 | 值 |
| --- | --- |
| 工作流 | X |
| 依赖 | WSX-02, WSX-03 |
| 交付物 | `avctl demo list` 列出所有内置样例；`avctl demo run <name>` 一键执行样例全流程（asset ingest → compile → run → trace export）；执行完输出结构化报告（timeline summary + event 统计 + trace 文件路径） |
| 验收标准 | `avctl demo list` 输出 3 个样例名称；`avctl demo run minimal-beat-show` 零手动干预完成；输出包含 timeline_entries 数量、total_events、trace_path；注册 demo.list / demo.run 两个新能力，总能力数 39 |
| 角色 | Tools Engineer |
| 规模 | L |

---

## 47. Phase 4 里程碑

### M19：契约加固

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSS-01 ~ WSS-04 全部达标 |
| 验收标准 | 补丁回退 RollbackCheckpoint 端到端可验证；storage 三子模块独立可测；Schema 扩展点注解就绪 |
| 交付物 | RollbackCheckpoint 类型、storage 子模块拆分、Schema x-stability 注解 |

### M20：外部控制 MVP

| 字段 | 值 |
| --- | --- |
| 触发条件 | M19 + WST-01 ~ WST-05 全部达标 |
| 验收标准 | MIDI/OSC 事件从 fixture 到 scheduler 到 trace 端到端可验证；control.* 能力在三入口可用 |
| 交付物 | ExternalControlEvent Schema + IR、NullControlAdapter、control 命名空间 4 能力、external_control_smoke.sh |

### M21：演出模板与场景包

| 字段 | 值 |
| --- | --- |
| 触发条件 | M20 + WSU-01 ~ WSU-04 全部达标 |
| 验收标准 | 模板注册 → 场景加载 → 场景切换 → trace 记录端到端可验证；template/scene 能力在三入口可用 |
| 交付物 | ShowTemplate + ScenePack Schema + IR、template_registry、scene_manager、template/scene 命名空间 4 能力 |

### M22：生态与稳定性

| 字段 | 值 |
| --- | --- |
| 触发条件 | M21 + WSV-01 ~ WSV-03 + WSW-01 ~ WSW-03 全部达标 |
| 验收标准 | 第三方 adapter 加载 → 执行 → trace 端到端可验证；128-bar 压力测试通过；fault injection → degrade → recovery 可验证；bench 基线存在 |
| 交付物 | 两个示范 adapter、FaultInjector 框架、scheduler_bench 基线、regression_suite.sh |

### M23：Showcase 样例与 Phase 4 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | M22 + WSX-01 ~ WSX-04 全部达标 |
| 验收标准 | `avctl demo list` 输出 3 个样例；`avctl demo run <name>` 对每个样例零手动干预完成；输出结构化报告；全量回归测试零失败；总能力数 39 |
| 交付物 | 3 个 Showcase 样例包、avctl demo 命令、Phase 4 验收报告 |

---

## 48. Phase 4 任务依赖图

```text
[M18: Phase 3 Complete]
       │
       ├──── WSS-01 ── WSS-04
       │     WSS-02
       │     WSS-03
       │                          ┐
       ├──── WST-01 ── WST-02 ── WST-03 ── WST-04 ── WST-05
       │                                    │
       ├──── WSU-01 ── WSU-02 ── WSU-03 ────┤── WSU-04
       │                          │
       ├──── WSV-01 ── WSV-02 ── WSV-03
       │
       ├──── WSW-01 ── WSW-02 ── WSW-03
       │
       └──── WSX-01 ── WSX-02 ──┐
                    │            ├── WSX-04
              WSX-03 ◄── WSS-04 ┘
```

## 49. Phase 4 推荐实施顺序

```text
Round 1 ─── 契约加固 + Schema/IR 基础
  │         WSS-01, WSS-02, WSS-03, WST-01, WSU-01
  │
Round 2 ─── 核心模块实现
  │         WSS-04, WST-02, WST-03, WSU-02, WSV-01, WSW-01
  │
Round 3 ─── 集成与稳定性
  │         WST-04, WST-05, WSU-03, WSU-04, WSV-02, WSV-03, WSW-02, WSW-03
  │
Round 4 ─── Showcase 样例与验收
  │         WSX-01, WSX-02, WSX-03, WSX-04
```

## 50. Phase 4 任务卡总览

> **Phase 4 状态：全部完成** — 23/23 卡 Done，M23 验收通过（2026-04-04）

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 | 状态 |
| --- | --- | --- | --- | --- | --- |
| WSS-01 | 补丁回退资源释放与状态恢复类型 | S | S | M19 | ✅ Done |
| WSS-02 | storage crate 拆分为三子模块 | S | M | M19 | ✅ Done |
| WSS-03 | Schema 扩展点标注与裸对象收口 | S | S | M19 | ✅ Done |
| WSS-04 | 补丁回退完整闭环 E2E 测试 | S | M | M19 | ✅ Done |
| WST-01 | 定义 ExternalControl 事件 Schema 与 IR 类型 | T | M | M20 | ✅ Done |
| WST-02 | ExternalControlAdapter trait 与 NullControlAdapter | T | M | M20 | ✅ Done |
| WST-03 | Scheduler 消费外部控制事件 | T | L | M20 | ✅ Done |
| WST-04 | CLI 与 MCP 新增 control 命名空间 | T | M | M20 | ✅ Done |
| WST-05 | 外部控制 E2E 测试 | T | M | M20 | ✅ Done |
| WSU-01 | 定义 ShowTemplate 与 ScenePack Schema 和 IR 类型 | U | M | M21 | ✅ Done |
| WSU-02 | 模板注册表与场景包加载器 | U | M | M21 | ✅ Done |
| WSU-03 | 场景切换运行时集成 | U | M | M21 | ✅ Done |
| WSU-04 | CLI 与 MCP 新增 template / scene 命名空间 | U | M | M21 | ✅ Done |
| WSV-01 | 示范性第三方音频分析器 Adapter | V | M | M22 | ✅ Done |
| WSV-02 | 示范性第三方视觉执行器 Adapter | V | M | M22 | ✅ Done |
| WSV-03 | 第三方 Adapter 集成 E2E 测试 | V | M | M22 | ✅ Done |
| WSW-01 | 长运行压力测试 Harness | W | M | M22 | ✅ Done |
| WSW-02 | 故障注入与降级恢复测试 | W | M | M22 | ✅ Done |
| WSW-03 | 回归测试套件与 bench 基线 | W | M | M22 | ✅ Done |
| WSX-01 | 定义样例包格式与首个样例资产集 | X | M | M23 | ✅ Done |
| WSX-02 | 构建 "Ambient Drift" 多段落样例 | X | M | M23 | ✅ Done |
| WSX-03 | 构建 "Live Patch Demo" 交互样例 | X | M | M23 | ✅ Done |
| WSX-04 | avctl demo 命令与一键演示运行器 | X | L | M23 | ✅ Done |

Phase 4 共计 23 张任务卡，覆盖 6 个 Workstream（S/T/U/V/W/X）和 5 个里程碑（M19-M23）。全部完成。

---

# Phase 5：执行面落地（Real Runtime Implementation）

> 日期：2026-04-04
> 前置：Phase 4 M23 验收通过，控制面完整闭环。Phase 5 聚焦将"确定性模拟/mock"替换为真实后端实现。
> 参考文档：13-产品技术栈决策依据建议书、20-音频输出模型与外部乐器接入设计、27-视觉后端抽象与多视角空间显示装置设计、28-音频输出后端抽象与空间扬声器矩阵装置设计、29-灯光输出后端抽象与离散式分布式灯光装置设计、19-调度运行时与补丁详细设计

## Phase 5 总览

| 维度 | 说明 |
| --- | --- |
| 目标 | 将执行面（音频、视觉、灯光）从 mock/stub 升级为真实后端；调度器从离线仿真升级为实时时钟驱动；进程分离基础设施就绪 |
| Workstream | Y（SuperCollider 音频桥接）、Z（wgpu 视觉渲染与 GLSL）、AA（Python 分析管线）、AB（DMX/ArtNet 灯光输出）、AC（实时调度器）、AD（进程分离与 IPC） |
| 里程碑 | M24–M29 |
| 任务卡 | 34 张 |
| 约束 | 每个 Workstream 必须实现 BackendAdapter trait 7 方法（已有 trait 定义于 Phase 3 WSO-01）；所有新后端与现有 trace/patch/evaluation 流程兼容；不引入分布式运行时（明确延后） |

---

## 51. Workstream Y：SuperCollider 音频桥接

### WSY-01 OSC 客户端与 scsynth 进程管理

| 字段 | 值 |
| --- | --- |
| 工作流 | Y |
| 依赖 | M23 (Phase 4 complete) |
| 交付物 | `crates/audio-bridge/` 新建 crate；`src/osc_client.rs` 基于 `rosc` crate 的 OSC 消息序列化/反序列化；`src/process_manager.rs` 负责 scsynth 进程启动、重启、关闭；`src/server_query.rs` 查询 scsynth 状态（/status, /version） |
| 验收标准 | `boot_scsynth()` 成功启动进程；`query_status()` 返回 OK；进程被 kill 后 `restart()` 在 2s 内恢复；5+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSY-02 命令翻译器：IR → OSC

| 字段 | 值 |
| --- | --- |
| 工作流 | Y |
| 依赖 | WSY-01 |
| 交付物 | `src/command_translator.rs`：AudioEvent → OSC 命令序列映射；覆盖三条音频路径：asset_playback（buffer load + synth launch）、synth_render（node create + param set）、midi_to_instrument（defer to adapter）；`src/node_mapping.rs`：action_id → synth_node_id 映射表 |
| 验收标准 | AssetPlaybackAction → 生成 `/b_allocRead` + `/s_new` OSC 消息；SynthRenderAction → 生成 `/s_new` + `/n_set` 消息；映射表可按 action_id 查询；6+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSY-03 Ack 收集与超时检测

| 字段 | 值 |
| --- | --- |
| 工作流 | Y |
| 依赖 | WSY-02 |
| 交付物 | `src/ack_collector.rs`：从 scsynth 回收 OSC `/done` `/fail` 消息，与 action_id 关联；`src/health_monitor.rs`：轮询 scsynth 状态，5s 无响应触发 degraded 状态 |
| 验收标准 | 发送 buffer load → 收到 `/done` ack → 状态更新为 loaded；scsynth 挂起时 health_monitor 在 5s 内检测；ack 超时后 action 标记 failed；4+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSY-04 实现 BackendAdapter for AudioScynthBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | Y |
| 依赖 | WSY-03 |
| 交付物 | `src/backend.rs`：AudioScynthBackend 实现 BackendAdapter trait 全部 7 方法（describe / prepare / apply_show_state / execute_payload / collect_status / apply_degrade / shutdown）；替换 scheduler 内现有 `FakeBackendClient` |
| 验收标准 | `describe()` 返回 backend 名称与版本；`execute_payload(AudioPayload)` → 翻译 → OSC 发送 → ack 收集；`shutdown()` 终止 scsynth 进程；`apply_degrade()` 停止创建新 node 但保持已有 node；7+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSY-05 音频桥接 E2E 与 Trace 集成

| 字段 | 值 |
| --- | --- |
| 工作流 | Y |
| 依赖 | WSY-04 |
| 交付物 | `tests/e2e/audio_bridge_smoke.sh`：compile → run（with scsynth backend）→ trace verify；trace 中包含 OSC command → ack 关联链；`avctl run start --backend=scsynth` 新增参数支持 |
| 验收标准 | E2E 脚本全流程通过（需本地已安装 scsynth）；trace 中每个 audio event 有对应 ack 记录；无 scsynth 时优雅退化到 reference backend + 诊断消息 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 52. Workstream Z：wgpu 视觉渲染与 GLSL

### WSZ-01 wgpu 设备初始化与窗口管理

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | M23 |
| 交付物 | `apps/visual-runtime/` 升级：`src/gpu/device_manager.rs` wgpu Device + Queue 初始化（macOS Metal 优先）；`src/gpu/window_manager.rs` winit 窗口创建、事件循环、Surface 管理；`Cargo.toml` 新增 wgpu + winit 依赖 |
| 验收标准 | 单窗口打开后渲染纯色背景 → 不崩溃；窗口可 resize；Surface 在 resize 时重建；3+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSZ-02 GLSL 编译管线（naga）

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-01 |
| 交付物 | `src/gpu/shader_compiler.rs`：GLSL 450 → naga Module → SPIR-V → wgpu::ShaderModule 完整链路；支持 vertex + fragment 双阶段；编译错误转化为 Diagnostic |
| 验收标准 | passthrough.vert + color.frag 编译成功；语法错误 GLSL 返回 Diagnostic 而非 panic；2+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSZ-03 渲染管线与 Uniform Buffer

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-02 |
| 交付物 | `src/gpu/render_pipeline.rs`：wgpu::RenderPipeline 创建（VS + FS + vertex layout + bind groups）；`src/gpu/buffer_manager.rs`：Uniform Buffer 分配、更新、对齐（256-byte）；`src/models/uniform.rs`：SceneUniformsGPU repr(C) 结构 |
| 验收标准 | 创建管线后渲染一个有色四边形到窗口；Uniform 更新后下一帧颜色变化；GPU buffer 对齐验证；4+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSZ-04 场景控制器与 Uniform 动画引擎

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-03 |
| 交付物 | `src/core/scene_controller.rs`：加载 SceneKernel（GLSL 对 + uniform 定义）、编译、持有活动管线；`src/core/uniform_automation.rs`：关键帧插值引擎，按 beat 进度驱动 uniform 参数变化 |
| 验收标准 | 加载 particles-basic kernel → 编译 → 渲染；16 beat 内 particle_color uniform 线性插值平滑；SceneController::load_scene() + compile_scene() 链路可用；4+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSZ-05 相机系统与多视口

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-04 |
| 交付物 | `src/core/camera_rig.rs`：CameraPreset → view/projection matrix 计算（基于 glam）；`src/core/viewport_composer.rs`：DisplayTopology → ViewSet，每个 view 关联 camera + target display；支持多窗口同场景不同视角 |
| 验收标准 | 3 个 CameraPreset 生成 3 组不同 view/projection 矩阵；同一场景在 2 个窗口渲染不同视角；矩阵计算单元测试通过；3+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSZ-06 实现 BackendAdapter for VisualWgpuBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-05 |
| 交付物 | `src/backend.rs`：VisualWgpuBackend 实现 BackendAdapter trait 7 方法；FlatDisplayBackend 为默认实现（单/多窗口）；替换现有 visual-runtime 的事件日志模式 |
| 验收标准 | `execute_payload(VisualPayload)` → 场景加载 + 渲染帧；`apply_show_state()` 更新 uniform + camera；`apply_degrade()` 切换到安全回退着色器；7+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSZ-07 基础粒子着色器（GLSL Demo）

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-03 |
| 交付物 | `assets/shaders/particles-basic/particles.vert`（顶点位置 + velocity 动画）；`assets/shaders/particles-basic/particles.frag`（颜色 + alpha 混合）；`assets/shaders/particles-basic/kernel.json`（SceneKernel 描述文件：uniform 定义、vertex 布局）|
| 验收标准 | naga 编译通过无错误；加载到 SceneController 后可渲染粒子场景；uniform particle_color 可被 automation 驱动；visual-runtime 集成测试通过 |
| 角色 | Content Engineer |
| 规模 | M |

### WSZ-08 视觉渲染 E2E 与 Trace 集成

| 字段 | 值 |
| --- | --- |
| 工作流 | Z |
| 依赖 | WSZ-06, WSZ-07 |
| 交付物 | `tests/e2e/visual_rendering_smoke.sh`：compile → run（with wgpu backend）→ trace verify；trace 记录每帧 scene ack 包含 render time、active uniforms；`avctl run start --visual-backend=wgpu` 参数支持 |
| 验收标准 | particles-basic 场景渲染 30+ 帧无崩溃；trace 中 visual ack 包含帧时间；无 GPU 环境优雅退化到 reference backend + 诊断消息 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 53. Workstream AA：Python 分析管线

### WSAA-01 Beat 与 Onset 检测（librosa）

| 字段 | 值 |
| --- | --- |
| 工作流 | AA |
| 依赖 | M23 |
| 交付物 | `python/vidodo_analysis/audio_analysis.py`：基于 librosa 的 beat_track + onset_detect；输出 BeatAnalysisResult（tempo_bpm, beats[], downbeats[], sections[]）；`python/vidodo_analysis/models/analysis_result.py`：Pydantic 模型 |
| 验收标准 | 对 128 BPM 测试 WAV 检测 tempo 误差 ≤ 2 BPM；beats 数组非空且单调递增；输出 JSON 可被 Python 与 Rust 解析；2+ Python 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAA-02 和声与调性分析（essentia）

| 字段 | 值 |
| --- | --- |
| 工作流 | AA |
| 依赖 | M23 |
| 交付物 | `python/vidodo_analysis/harmony_analysis.py`：基于 essentia 的调性检测 + 和弦跟踪；输出 HarmonyAnalysisResult（estimated_key, chords[]）|
| 验收标准 | C 大调测试音频返回 "C major"；chords 数组含时间戳与和弦标签；2+ Python 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAA-03 MIDI 符号解析（music21）

| 字段 | 值 |
| --- | --- |
| 工作流 | AA |
| 依赖 | M23 |
| 交付物 | `python/vidodo_analysis/midi_analysis.py`：基于 music21 的 MIDI 文件解析；输出 MidiAnalysisResult（notes[], programs[], key_signature, time_signature）|
| 验收标准 | 测试 MIDI 文件的音符数与预期一致；调号和拍号正确提取；2+ Python 测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSAA-04 Python CLI 与 Rust 子进程桥接

| 字段 | 值 |
| --- | --- |
| 工作流 | AA |
| 依赖 | WSAA-01, WSAA-02, WSAA-03 |
| 交付物 | `python/vidodo_analysis/cli.py`：Click CLI 入口（beat-detect / harmony-detect / midi-parse 三个子命令）；`python/requirements.txt` + `python/setup.py`；`crates/storage/src/analysis_bridge.rs`：Rust 侧 `PythonAnalysisRunner` 通过 `std::process::Command` 调用 Python CLI，解析 JSON stdout |
| 验收标准 | `python -m vidodo_analysis beat-detect test.wav` 输出 JSON；Rust `PythonAnalysisRunner::beat_detect()` 返回结构化结果；Python 不可用时返回明确错误而非 panic；3+ 测试（1 Python + 2 Rust） |
| 角色 | Tools Engineer |
| 规模 | M |

### WSAA-05 分析结果注入 IR 编译链路

| 字段 | 值 |
| --- | --- |
| 工作流 | AA |
| 依赖 | WSAA-04 |
| 交付物 | `crates/compiler/` 扩展：编译时检测资产分析缓存，将 beat_map / key / section_boundaries 注入 PerformanceIr；`avctl asset analyze` 新增命令触发分析并写入缓存 |
| 验收标准 | asset analyze → 缓存 JSON 生成；compile run → PerformanceIr 包含 beat_map 字段（非硬编码）；无分析缓存时编译仍成功但输出 warning；3+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

---

## 54. Workstream AB：DMX/ArtNet 灯光输出

### WSAB-01 DMX 帧构建与 ArtNet 包序列化

| 字段 | 值 |
| --- | --- |
| 工作流 | AB |
| 依赖 | M23 |
| 交付物 | `crates/lighting-bridge/` 新建 crate；`src/dmx_frame.rs`：512 通道 DMX 帧构建与读写；`src/artnet_packet.rs`：ArtNet DMX 包（opcode 0x5000）序列化/反序列化；`src/udp_sender.rs`：UDP socket 发送逻辑 |
| 验收标准 | DMX 帧正确填充亮度通道（intensity 0.8 → channel 值 204）；ArtNet 包序列化后 header 字段正确；UDP 发送到 localhost 无错误；4+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSAB-02 灯具拓扑与地址映射

| 字段 | 值 |
| --- | --- |
| 工作流 | AB |
| 依赖 | WSAB-01 |
| 交付物 | `src/fixture_topology.rs`：FixtureEndpoint 模型（fixture_id, universe, address, capabilities）；FixtureBusTopology 集合与按 ID 查询；地址计算（universe + channel offset） |
| 验收标准 | 加载灯具拓扑 JSON；按 fixture_id 查询返回正确 universe/address；dimmer 灯具占 1 通道、RGB 灯具占 4 通道（dimmer+R+G+B）；3+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | S |

### WSAB-03 Cue → DMX 翻译器

| 字段 | 值 |
| --- | --- |
| 工作流 | AB |
| 依赖 | WSAB-02 |
| 交付物 | `src/cue_translator.rs`：CueEntry → DMX 帧翻译；根据 fixture capabilities 填充 dimmer/RGB/pan/tilt 通道；支持多 fixture_group 跨 universe 输出 |
| 验收标准 | CueEntry(intensity=0.5, color=[1,0,0]) → RGB fixture：dimmer=127, R=255, G=0, B=0；跨 2 个 universe 的 fixture_group 生成 2 个 DMX 帧；4+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | M |

### WSAB-04 实现 BackendAdapter for FixtureBusBackend

| 字段 | 值 |
| --- | --- |
| 工作流 | AB |
| 依赖 | WSAB-03 |
| 交付物 | `src/backend.rs`：FixtureBusBackend 实现 BackendAdapter trait 7 方法；替换现有 lighting-runtime 的事件日志模式 |
| 验收标准 | `prepare()` 加载灯具拓扑；`execute_payload(LightingPayload)` → CueSet → DMX → ArtNet UDP 发送；`apply_degrade()` 停止发送新命令但保持最后状态；`shutdown()` 将所有通道归零（blackout）；6+ 单元测试 |
| 角色 | Runtime Engineer |
| 规模 | L |

### WSAB-05 灯光输出 E2E 与 Trace 集成

| 字段 | 值 |
| --- | --- |
| 工作流 | AB |
| 依赖 | WSAB-04 |
| 交付物 | `tests/e2e/lighting_output_smoke.sh`：compile → run（with fixture-bus backend）→ trace verify；trace 记录 cue → DMX frame 关联；`avctl run start --lighting-backend=fixture-bus` 参数支持 |
| 验收标准 | E2E 全流程通过（可用 mock UDP listener 验证包内容）；trace 中每个 lighting event 有对应 cue ack；无灯光硬件时优雅退化到 reference backend + 诊断消息 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 55. Workstream AC：实时调度器

### WSAC-01 实时 MusicalClock（wall-clock 驱动）

| 字段 | 值 |
| --- | --- |
| 工作流 | AC |
| 依赖 | M23 |
| 交付物 | `crates/scheduler/src/realtime_clock.rs`：基于 `std::time::Instant` 的 wall-clock 驱动 MusicalClock；按真实经过时间推进 bar/beat/phrase/section；支持 tempo 变化即时生效 |
| 验收标准 | 120 BPM 下 500ms 实际等待后 beat 推进 1.0 ± 0.01；tempo 从 120 切到 60 后 beat 推进速率减半；与现有 offline clock 共享 MusicalTime 类型；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAC-02 Transport 状态机（Play/Pause/Seek）

| 字段 | 值 |
| --- | --- |
| 工作流 | AC |
| 依赖 | WSAC-01 |
| 交付物 | `crates/scheduler/src/transport.rs`：TransportState 枚举（Playing / Paused / Stopped）；play() / pause() / seek(beat) / set_tempo(bpm) 方法；seek 后 clock 立即重定位 |
| 验收标准 | play → pause → play 周期后 beat 仅在 play 期间推进；seek(beat=16) 后 clock 报告 bar=4 beat=0（4/4 拍）；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAC-03 实时 Lookahead 与事件分发

| 字段 | 值 |
| --- | --- |
| 工作流 | AC |
| 依赖 | WSAC-02 |
| 交付物 | `crates/scheduler/src/realtime_dispatch.rs`：每 tick（1ms 粒度）检查 lookahead 窗口内到期事件 → 按类型分发到 Audio/Visual/Lighting 通道；`src/event_queue.rs`：lock-free 事件队列（crossbeam SegQueue） |
| 验收标准 | 4 bar timeline 在实时模式下所有 beat edge 事件按序分发；事件分发延迟 ≤ 2ms（从 edge 检测到 dispatch）；与 mock backend 集成验证无事件丢失；5+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | L |

### WSAC-04 实时补丁窗口与回退

| 字段 | 值 |
| --- | --- |
| 工作流 | AC |
| 依赖 | WSAC-03 |
| 交付物 | 扩展 scheduler 实时模式：补丁仅在 beat edge 且 safe zone（距下一 transport change ≥ 500ms）内激活；超时未激活自动回退；trace 记录补丁窗口开关事件 |
| 验收标准 | 补丁在 beat edge 激活成功；在非 safe zone 提交补丁时被延迟到下一窗口；窗口关闭未激活的补丁自动回退；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAC-05 实时调度 E2E 与双模式切换

| 字段 | 值 |
| --- | --- |
| 工作流 | AC |
| 依赖 | WSAC-04 |
| 交付物 | `avctl run start --mode=realtime` 新增参数（默认保持 offline）；`tests/e2e/realtime_scheduler_smoke.sh`：realtime 模式下 4 bar show → trace verify；确保 offline 模式功能不退化 |
| 验收标准 | realtime 模式下 4 bar show 在约 8s（120 BPM）内完成；trace 事件时间戳反映真实 wall-clock；offline 模式回归测试全通过；E2E 脚本全通过 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 56. Workstream AD：进程分离与 IPC

### WSAD-01 IPC 消息类型与序列化

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | M23 |
| 交付物 | `crates/ipc/` 新建 crate；`src/messages.rs`：RuntimeMessage 枚举（Action / Patch / Transport / Terminate）+ RuntimeAck 枚举（ExecutionAck / HealthStatus）；全部含 correlation_id 字段；serde JSON 序列化 |
| 验收标准 | 所有消息类型 serde round-trip 通过；correlation_id 在 message → ack 链路中保持一致；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSAD-02 线程间通道基础设施

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | WSAD-01 |
| 交付物 | `src/channel.rs`：RuntimeChannel 结构（paired crossbeam Sender/Receiver）；非阻塞 send/try_recv 包装；错误处理（channel full → back-pressure，disconnected → degrade） |
| 验收标准 | 发送 100 条消息全部按序接收；channel 满时 send 返回 back-pressure 错误而非阻塞；receiver 断开时 sender 检测到；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | S |

### WSAD-03 Scheduler → Runtime 线程调度集成

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | WSAD-02, WSAC-03 |
| 交付物 | `src/scheduler_ipc.rs`：SchedulerWithIPC 包装器；每 tick 将 RuntimeEvent 分发到对应 runtime channel；每 tick 收集 ack + health sample；`src/runtime_thread.rs`：ThreadRuntimeContainer 负责 spawn/join runtime 线程 |
| 验收标准 | 3 个 mock runtime 线程启动 → scheduler 分发 4 个 event → 全部 ack 收集 → trace 记录完整因果链；线程正常 shutdown 无 hang；4+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | L |

### WSAD-04 因果链追踪与诊断

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | WSAD-03 |
| 交付物 | 所有 IPC 消息携带 message_id + parent_id + correlation_id；TraceWriter 记录完整因果链（scheduler tick → event dispatch → runtime ack）；`avctl trace causation --correlation-id <id>` 新增查询命令 |
| 验收标准 | 从 beat edge 到 backend ack 的完整因果链可通过 trace 查询重建；correlation_id 贯穿 3 跳（scheduler → channel → runtime）；2+ 测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAD-05 降级与容错

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | WSAD-03 |
| 交付物 | `src/resilience.rs`：runtime 线程无响应 500ms → 标记 degraded → 触发 health_monitor 降级决策；runtime 线程 panic → 检测 → 重启或安全回退 |
| 验收标准 | mock runtime 挂起 → scheduler 在 500ms 内检测 → degrade event 写入 trace；runtime panic → scheduler 继续运行、其余 runtime 不受影响；3+ 单元测试 |
| 角色 | Core Engineer |
| 规模 | M |

### WSAD-06 进程分离 E2E 与压力测试

| 字段 | 值 |
| --- | --- |
| 工作流 | AD |
| 依赖 | WSAD-04, WSAD-05 |
| 交付物 | `tests/e2e/ipc_integration_smoke.sh`：3 runtime 线程 + scheduler → 8 bar show → trace 因果链验证；`tests/e2e/ipc_stress.sh`：1000 event/sec × 10s → 无消息丢失 → 内存稳定 |
| 验收标准 | 8 bar show 所有事件有 ack；因果链完整可查；压力测试 10000 event 零丢失；内存增长 ≤ 10%；E2E 脚本全通过 |
| 角色 | QA Engineer |
| 规模 | M |

---

## 57. Phase 5 里程碑

### M24：SuperCollider 音频桥接

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSY-01 ~ WSY-05 全部达标 |
| 验收标准 | scsynth 可启动/关闭/重启；AudioEvent → OSC → scsynth → ack 端到端可验证；trace 记录 OSC command-ack 关联；无 scsynth 时优雅退化 |
| 交付物 | audio-bridge crate、AudioScynthBackend、E2E 脚本 |

### M25：wgpu 视觉渲染

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSZ-01 ~ WSZ-08 全部达标 |
| 验收标准 | wgpu 窗口渲染粒子场景 30+ 帧无崩溃；GLSL → SPIR-V 编译链路完整；多视口多摄像机工作；uniform 动画平滑；BackendAdapter 集成可用；无 GPU 时优雅退化 |
| 交付物 | visual-runtime 升级（wgpu 后端）、particles-basic 着色器、3+ CalibrationProfile fixture |

### M26：Python 分析管线

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSAA-01 ~ WSAA-05 全部达标 |
| 验收标准 | librosa beat detection 误差 ≤ 2 BPM；essentia 调性检测正确；music21 MIDI 解析完整；Rust 子进程桥接端到端可用；分析结果注入 IR 编译链路 |
| 交付物 | python/vidodo_analysis 包、analysis_bridge Rust 模块、avctl asset analyze 命令 |

### M27：DMX/ArtNet 灯光输出

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSAB-01 ~ WSAB-05 全部达标 |
| 验收标准 | DMX 帧构建正确；ArtNet UDP 包可发送；CueSet → DMX 翻译完整；BackendAdapter 集成可用；trace 记录 cue-ack 关联；无硬件时优雅退化 |
| 交付物 | lighting-bridge crate、FixtureBusBackend、fixture topology fixtures |

### M28：实时调度器

| 字段 | 值 |
| --- | --- |
| 触发条件 | WSAC-01 ~ WSAC-05 全部达标 |
| 验收标准 | wall-clock 驱动的 MusicalClock 精度 ≤ 1ms；transport play/pause/seek 正确；实时事件分发延迟 ≤ 2ms；补丁窗口逻辑正确；offline 模式零退化；双模式切换可用 |
| 交付物 | realtime_clock、transport、realtime_dispatch 模块、--mode=realtime 参数 |

### M29：进程分离与 Phase 5 验收

| 字段 | 值 |
| --- | --- |
| 触发条件 | M24 ~ M28 + WSAD-01 ~ WSAD-06 全部达标 |
| 验收标准 | IPC 消息类型完整；线程间通道可靠（零消息丢失）；因果链追踪端到端可查；降级容错在 500ms 内触发；压力测试 10000 event 零丢失；全量回归测试零失败 |
| 交付物 | ipc crate、SchedulerWithIPC、因果链追踪命令、Phase 5 验收报告 |

---

## 58. Phase 5 任务依赖图

```text
[M23: Phase 4 Complete]
       │
       ├──── WSY-01 ── WSY-02 ── WSY-03 ── WSY-04 ── WSY-05
       │                                                 │
       ├──── WSZ-01 ── WSZ-02 ── WSZ-03 ── WSZ-04 ── WSZ-05 ── WSZ-06 ── WSZ-08
       │                    │                                      │
       │                    └── WSZ-07 ─────────────────────────── ┘
       │
       ├──── WSAA-01 ──┐
       │     WSAA-02 ──┼── WSAA-04 ── WSAA-05
       │     WSAA-03 ──┘
       │
       ├──── WSAB-01 ── WSAB-02 ── WSAB-03 ── WSAB-04 ── WSAB-05
       │
       ├──── WSAC-01 ── WSAC-02 ── WSAC-03 ── WSAC-04 ── WSAC-05
       │                              │
       └──── WSAD-01 ── WSAD-02 ──── WSAD-03 ── WSAD-04
                                       │        WSAD-05
                                       └──────── WSAD-06
```

## 59. Phase 5 推荐实施顺序

```text
Round 1 ─── 基础设施与独立模块
  │         WSY-01, WSZ-01, WSAA-01, WSAA-02, WSAA-03, WSAB-01, WSAC-01, WSAD-01
  │         （7 条并行启动线，全部仅依赖 M23）
  │
Round 2 ─── 核心翻译与编译
  │         WSY-02, WSZ-02, WSZ-07, WSAA-04, WSAB-02, WSAC-02, WSAD-02
  │
Round 3 ─── 管线集成与渲染
  │         WSY-03, WSZ-03, WSAA-05, WSAB-03, WSAC-03, WSAD-03
  │
Round 4 ─── BackendAdapter 实现
  │         WSY-04, WSZ-04, WSZ-05, WSAB-04, WSAC-04, WSAD-04, WSAD-05
  │
Round 5 ─── E2E 集成与验收
  │         WSY-05, WSZ-06, WSZ-08, WSAB-05, WSAC-05, WSAD-06
  │
并行机会：
  - Workstream Y/Z/AA/AB 在 Round 1-3 完全独立
  - WSAC 与 WSAD 在 Round 3 开始耦合（WSAD-03 依赖 WSAC-03）
  - WSAA（Python）不阻塞其他 Workstream，可作为独立线独立推进
  - WSZ-07（GLSL Demo）仅需 WSZ-02 完成后即可启动，与主渲染管线并行
```

## 60. Phase 5 任务卡总览

> **Phase 5 状态：全部完成** — 34/34 卡 Done，M29 验收通过（2026-04-05）

| 卡号 | 标题 | 工作流 | 规模 | 里程碑 | 状态 |
| --- | --- | --- | --- | --- | --- |
| WSY-01 | OSC 客户端与 scsynth 进程管理 | Y | M | M24 | ✅ Done |
| WSY-02 | 命令翻译器：IR → OSC | Y | L | M24 | ✅ Done |
| WSY-03 | Ack 收集与超时检测 | Y | M | M24 | ✅ Done |
| WSY-04 | 实现 BackendAdapter for AudioScynthBackend | Y | L | M24 | ✅ Done |
| WSY-05 | 音频桥接 E2E 与 Trace 集成 | Y | M | M24 | ✅ Done |
| WSZ-01 | wgpu 设备初始化与窗口管理 | Z | M | M25 | ✅ Done |
| WSZ-02 | GLSL 编译管线（naga） | Z | M | M25 | ✅ Done |
| WSZ-03 | 渲染管线与 Uniform Buffer | Z | L | M25 | ✅ Done |
| WSZ-04 | 场景控制器与 Uniform 动画引擎 | Z | L | M25 | ✅ Done |
| WSZ-05 | 相机系统与多视口 | Z | M | M25 | ✅ Done |
| WSZ-06 | 实现 BackendAdapter for VisualWgpuBackend | Z | L | M25 | ✅ Done |
| WSZ-07 | 基础粒子着色器（GLSL Demo） | Z | M | M25 | ✅ Done |
| WSZ-08 | 视觉渲染 E2E 与 Trace 集成 | Z | M | M25 | ✅ Done |
| WSAA-01 | Beat 与 Onset 检测（librosa） | AA | M | M26 | ✅ Done |
| WSAA-02 | 和声与调性分析（essentia） | AA | M | M26 | ✅ Done |
| WSAA-03 | MIDI 符号解析（music21） | AA | S | M26 | ✅ Done |
| WSAA-04 | Python CLI 与 Rust 子进程桥接 | AA | M | M26 | ✅ Done |
| WSAA-05 | 分析结果注入 IR 编译链路 | AA | M | M26 | ✅ Done |
| WSAB-01 | DMX 帧构建与 ArtNet 包序列化 | AB | M | M27 | ✅ Done |
| WSAB-02 | 灯具拓扑与地址映射 | AB | S | M27 | ✅ Done |
| WSAB-03 | Cue → DMX 翻译器 | AB | M | M27 | ✅ Done |
| WSAB-04 | 实现 BackendAdapter for FixtureBusBackend | AB | L | M27 | ✅ Done |
| WSAB-05 | 灯光输出 E2E 与 Trace 集成 | AB | M | M27 | ✅ Done |
| WSAC-01 | 实时 MusicalClock（wall-clock 驱动） | AC | M | M28 | ✅ Done |
| WSAC-02 | Transport 状态机（Play/Pause/Seek） | AC | M | M28 | ✅ Done |
| WSAC-03 | 实时 Lookahead 与事件分发 | AC | L | M28 | ✅ Done |
| WSAC-04 | 实时补丁窗口与回退 | AC | M | M28 | ✅ Done |
| WSAC-05 | 实时调度 E2E 与双模式切换 | AC | M | M28 | ✅ Done |
| WSAD-01 | IPC 消息类型与序列化 | AD | S | M29 | ✅ Done |
| WSAD-02 | 线程间通道基础设施 | AD | S | M29 | ✅ Done |
| WSAD-03 | Scheduler → Runtime 线程调度集成 | AD | L | M29 | ✅ Done |
| WSAD-04 | 因果链追踪与诊断 | AD | M | M29 | ✅ Done |
| WSAD-05 | 降级与容错 | AD | M | M29 | ✅ Done |
| WSAD-06 | 进程分离 E2E 与压力测试 | AD | M | M29 | ✅ Done |

Phase 5 共计 34 张任务卡，覆盖 6 个 Workstream（Y/Z/AA/AB/AC/AD）和 6 个里程碑（M24-M29）。全部 done（2026-04-05）。
