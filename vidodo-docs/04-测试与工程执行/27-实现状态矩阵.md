# 实现状态矩阵

> 日期：2026-04-02（Phase-next 更新）  
> 基于第三方审计报告建议创建，用于明确标注各模块当前实现状态，避免文档未来态与代码现态混淆。

## 状态图例

| 状态 | 含义 |
|------|------|
| **已实现并可验证** | 代码存在，测试覆盖，E2E 可演示 |
| **已设计未实现** | Schema/文档已定义，代码仅占位或不存在 |
| **明确延后** | 相关任务卡标记为后续阶段 |

## Rust Crates 状态

| Crate | 状态 | 说明 |
|-------|------|------|
| `ir` | 已实现并可验证 | 50+ 公共类型，覆盖 planning/compilation/runtime/patch 全域 |
| `validator` | 已实现并可验证 | 11 个检查码（VAL-001 ~ VAL-010），8 个单元测试覆盖正/负路径 |
| `compiler` | 已实现并可验证 | PlanBundle → CompiledRevision 确定性编译，4 个测试 + benchmark |
| `scheduler` | 已实现并可验证 | 离线仿真调度，BackendClient trait 抽象，事件流生成，patch 事件集成 |
| `patch-manager` | 已实现并可验证 | check/apply/rollback 三阶段，10 个检查码，7 个测试覆盖正/负路径 |
| `storage` | 已实现并可验证 | SQLite 注册表、资产 ingest、分析缓存、WAV 探针，8 个测试 |
| `trace` | 已实现并可验证 | JSONL 事件写入、manifest 生成、加载回放 |
| `evaluation` | 已实现并可验证 | 评估报告生成：sync/transition/resource_stability/patch_safety 四维评分，4 个测试 |

## Apps 状态

| App | 状态 | 说明 |
|-----|------|------|
| `avctl` | 已实现并可验证 | CLI 主入口，覆盖 doctor/asset/plan/compile/run/patch/trace/eval 全链路 |
| `core-service` | 已设计未实现 | 占位 main.rs，能力层服务化待 Phase 1 |
| `mcp-adapter` | 已设计未实现 | 占位 main.rs，MCP 工具面待 Phase 1 |
| `visual-runtime` | 已设计未实现 | 占位 main.rs，真实视觉渲染待 Phase 1+ |

## Schema 状态

| Schema 分类 | 状态 | 说明 |
|-------------|------|------|
| `common/` (6 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖 |
| `planning/` (4 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖 |
| `runtime/` (7 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖（审计后补全） |
| `ir/` (9 个) | 已实现并可验证 | 核心 IR 有 fixture，拓扑/资产 IR 审计后补全 |
| `patch/` (1 个) | 已实现并可验证 | scope 已对齐为结构化对象 |
| `asset/` (2 个) | 已实现并可验证 | fixture 覆盖 |
| `capability/` (1 个) | 已实现并可验证 | valid+invalid fixture 审计后补全 |
| `mcp-tools/` (1 个) | 已实现并可验证 | $id 已修正，fixture 审计后补全 |
| `trace/` (1 个) | 已实现并可验证 | fixture 覆盖 |
| `lighting/` (2 个) | 已设计未实现 | Schema 存在，有 valid fixture，运行时待 Phase 1+ |

## 文档与代码对齐状态

| 文档描述的能力 | 代码实现状态 | 差距说明 |
|---------------|-------------|---------|
| 外部规划 → 确定性编译 | ✅ 已闭环 | plan validate → compile run 完整可用 |
| 统一时间模型 (MusicalTime) | ✅ 已闭环 | 事件/调度/trace 全部基于 MusicalTime |
| 补丁提案/检查/应用 | ✅ 已闭环 | check → submit → apply 链路完整 |
| 补丁回退 | ⚠️ 部分闭环 | 对象级回退已实现，资源句柄释放/状态恢复待深化 |
| 追踪/证据链 | ✅ 已闭环 | events.jsonl + summary + show-state + manifest + evaluation 完整 |
| 资产 ingest 与分析 | ✅ 已闭环 | WAV ingest + beat-track + cache + registry 完整 |
| 能力层服务化 | ❌ 未实现 | core-service 占位，Phase 1 |
| MCP 工具面 | ❌ 未实现 | mcp-adapter 占位，Phase 1 |
| 真实视觉运行时 | ❌ 未实现 | visual-runtime 占位，Phase 1+ |
| 灯光协议 | ❌ 未实现 | Schema 存在，代码待开发 |
| 分布式运行时 | ❌ 未实现 | 明确延后 |

## 测试覆盖状态

| 测试类型 | 覆盖情况 | 补全建议 |
|----------|---------|---------|
| Schema fixture (valid) | 67 个 fixture 全部通过 | ✅ 已补全 |
| Schema fixture (invalid) | 覆盖核心负路径 | ✅ 已补全 |
| Rust 单元测试 | 33 个测试全部通过 | ✅ 正/负路径已补全，evaluation + backend trait 已覆盖 |
| E2E smoke (phase0) | Happy path 完整（含 eval） | ✅ 已补全 |
| E2E smoke (asset) | Happy path 完整 | 后续需补充 ingest 失败场景 |
| E2E negative paths | 11 个负路径场景全部通过 | ✅ 新增：缺参/未知 show/revision 不匹配/scope 越界/回退未知 patch |
| Benchmark | compile 基准已建立 | 随功能增加扩展 |
