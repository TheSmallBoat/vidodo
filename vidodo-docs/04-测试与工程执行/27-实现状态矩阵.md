# 实现状态矩阵

> 日期：2026-04-03（Phase 1 Round 3 — WSJ-03 更新）  
> 基于第三方审计报告建议创建，用于明确标注各模块当前实现状态，避免文档未来态与代码现态混淆。

## 状态图例

| 状态 | 含义 |
|------|------|
| **已实现并可验证** | 代码存在，测试覆盖，E2E 可演示 |
| **已设计未实现** | Schema/文档已定义，代码仅占位或不存在 |
| **明确延后** | 相关任务卡标记为后续阶段 |

## Rust Crates 状态

| Crate | 状态 | 说明 |
|-------|------|------|
| `ir` | 已实现并可验证 | 50+ 公共类型，含 ResourceSample、ExportArtifactRecord、SectionBoundary、SectionSegmentationAnalysis、CapabilityDescriptor、CapabilityRequest、OperationTicket、LightingFixture、LightingTopology、LightingCue、CueSet、LightingEvent；CompiledRevision 含 lighting_topology + cue_sets 字段；PlanBundle 含 lighting 数据，3 个 serde round-trip 测试 |
| `validator` | 已实现并可验证 | 11 个检查码（VAL-001 ~ VAL-010），8 个单元测试覆盖正/负路径 |
| `compiler` | 已实现并可验证 | 编译 + revision.rs 模块：candidate/publish/archive 生命周期，SQLite 支持，7 个测试 |
| `scheduler` | 已实现并可验证 | 离线仿真 + clock.rs + lookahead.rs + show_state.rs（ShowState 构建与快照），13 个测试 |
| `patch-manager` | 已实现并可验证 | check/apply/rollback/deferred_rollback 四阶段，12 个检查码（PAT-001~010 + PAT-012），lighting_cue patch_class 支持灯光 fixture 可用性校验，11 个测试覆盖正/负/延迟回退/灯光路径 |
| `storage` | 已实现并可验证 | SQLite 注册表（含 revisions 表）、资产 ingest、beat_track + section_segmentation 双分析器、WAV 探针，8 个测试 |
| `trace` | 已实现并可验证 | JSONL 事件 + patch-decisions + resource-samples + export_audio + filter_events_by_bar + manifest，5 个测试 |
| `capability` | 已实现并可验证 | 能力注册表（21 个能力描述符）、路由器、操作跟踪器（真实时间戳 + start_if_async）、MCP 工具映射（21 条 + resolve_mcp_tool），13 个测试 |
| `evaluation` | 已实现并可验证 | 评估报告生成：sync/transition/resource_stability/patch_safety 四维评分，4 个测试 |

## Apps 状态

| App | 状态 | 说明 |
|-----|------|------|
| `avctl` | 已实现并可验证 | CLI 主入口，覆盖 doctor/asset/plan/compile/revision/run/patch(含 deferred-rollback)/trace(含 bar 过滤)/eval/export/system(capabilities) 全链路 |
| `core-service` | 已实现并可验证 | axum HTTP 服务器：GET /health、GET /capabilities、POST /capability/{id} 路由；4 个集成测试 |
| `mcp-adapter` | 已设计未实现 | 占位 main.rs，MCP 工具面待 Phase 1 |
| `visual-runtime` | 已实现并可验证 | 消费 visual+timing 事件，输出 scene ack 日志（visual-acks.json），3 个测试 |
| `lighting-runtime` | 已实现并可验证 | 消费 lighting+timing 事件，输出 cue ack 日志（lighting-acks.json），4 个测试 |

## Schema 状态

| Schema 分类 | 状态 | 说明 |
|-------------|------|------|
| `common/` (6 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖 |
| `planning/` (4 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖 |
| `runtime/` (7 个) | 已实现并可验证 | 全部有 valid+invalid fixture 覆盖（审计后补全） |
| `ir/` (9 个) | 已实现并可验证 | 核心 IR 有 fixture，拓扑/资产 IR 审计后补全 |
| `patch/` (1 个) | 已实现并可验证 | scope 已对齐为结构化对象 |
| `asset/` (2 个) | 已实现并可验证 | fixture 覆盖 |
| `capability/` (2 个) | 已实现并可验证 | adapter-plugin-manifest + capability-descriptor；valid+invalid fixture 覆盖 |
| `mcp-tools/` (1 个) | 已实现并可验证 | $id 已修正，21-tool 完整 fixture + annotations 覆盖 |
| `trace/` (1 个) | 已实现并可验证 | fixture 覆盖 |
| `lighting/` (2 个) | 已实现并可验证 | Schema + valid/invalid fixture 覆盖，IR 对齐，lighting-runtime 消费 |

## 文档与代码对齐状态

| 文档描述的能力 | 代码实现状态 | 差距说明 |
|---------------|-------------|---------|
| 外部规划 → 确定性编译 | ✅ 已闭环 | plan validate → compile run 完整可用 |
| 统一时间模型 (MusicalTime) | ✅ 已闭环 | MusicalClock 模块化，事件/调度/trace 全部基于 MusicalTime |
| 补丁提案/检查/应用 | ✅ 已闭环 | check → submit → apply 链路完整 |
| 补丁回退（含延迟回退） | ✅ 已闭环 | 即时回退 + deferred_rollback（异常触发，trace 记录），CLI 支持 |
| 追踪/证据链 | ✅ 已闭环 | events + patch-decisions + resource-samples + summary + evaluation + export + bar 过滤查询 |
| 离线导出 | ✅ 已闭环 | export audio 生成 WAV + ExportArtifactRecord，关联 trace bundle |
| 版本管理 | ✅ 已闭环 | candidate → published → archived，SQLite revisions 表，CLI revision list/publish/archive |
| 调度器模块化 | ✅ 已闭环 | clock.rs + lookahead.rs + show_state.rs 三模块化 |
| 资产 ingest 与分析 | ✅ 已闭环 | WAV ingest + beat_track + section_segmentation 双分析器 + cache + registry |
| 视觉运行时骨架 | ✅ 已闭环 | visual-runtime 消费事件输出 scene acks |
| 能力层抽象 | ✅ 已闭环 | capability crate 含 21 个能力描述符、路由器、操作跟踪器（真实时间戳 + async 门控）、MCP 映射表；avctl system capabilities 可用 |
| 能力层服务化（HTTP） | ✅ 骨架已闭环 | core-service axum 服务器，health/capabilities/invoke 三条路由，POST /capability/{id} 可路由到 RouteTarget；WSH-06~07 待填充实际处理器 |
| MCP 工具面 | 🔵 映射已建立 | 21 条 tool↔capability 映射 + resolve 函数 + 完整 fixture；mcp-adapter 占位，Phase 1 WSI-02~04 |
| 灯光协议 | ✅ 已闭环 | lighting IR 类型 + LightingEvent + lighting-runtime 消费事件输出 cue acks + 灯光 patch 检查（PAT-012） |
| 分布式运行时 | ❌ 未实现 | 明确延后 |

## 测试覆盖状态

| 测试类型 | 覆盖情况 | 补全建议 |
|----------|---------|---------|
| Schema fixture (valid) | 75 个 fixture 全部通过 | ✅ 新增 lighting full + cue-set full + MCP 21-tool fixture |
| Schema fixture (invalid) | 覆盖核心负路径 | ✅ 已补全 |
| Rust 单元测试 | 83 个测试全部通过 | ✅ 新增：IR lighting serde 3、OperationTracker 时间戳 2 + async 门控 2、MCP 映射 3、lighting-runtime 4、灯光 patch 检查 2、core-service HTTP 4 |
| E2E smoke (phase0) | Happy path 完整（含 deferred-rollback + bar 过滤 + visual-runtime + system capabilities） | ✅ 新增 system capabilities |
| E2E smoke (asset) | Happy path 完整 | 后续需补充 ingest 失败场景 |
| E2E negative paths | 24 个负路径场景全部通过 | ✅ 新增：system capabilities 成功、system 非法子命令 |
| Benchmark | compile 基准已建立 | 随功能增加扩展 |
